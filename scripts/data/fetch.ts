/**
 * `pnpm data:fetch` — 원본 데이터 다운로드 → data-src/raw/ (T0b에서 구현).
 * HYG(codeberg astronexus/hyg), OpenNGC, d3-celestial, NASA SVS 은하수.
 */
console.error('data:fetch — Task 0b에서 구현됩니다. (plan/task-00-setup-and-data.md §3.3)');
process.exit(2);
