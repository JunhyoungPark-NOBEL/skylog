# DECISIONS — 결정 기록 (ADR 요약)

형식: `D-번호 · 날짜 · 제목` → 맥락 / 결정 / 결과·영향. 새 결정은 아래에 추가하고, 번복하면 원 항목에 "→ D-xx로 대체" 표기.

## D-001 · 2026-09-06 · 플랫폼은 PWA
- 맥락: GPS·나침반·자이로가 필요하고, 빠른 반복 개발과 URL 배포가 중요.
- 결정: React + TypeScript + Vite + Three.js 기반 PWA. 네이티브 포장(Capacitor)은 보류(D9).
- 영향: 센서는 웹 API(DeviceOrientation/Generic Sensor)에 의존 → iOS 제약을 보정 기능으로 상쇄. HTTPS 배포 필수.

## D-002 · 2026-09-06 · 저장은 로컬 우선
- 결정: Dexie(IndexedDB) + JSON 내보내기/가져오기. 모든 레코드에 `updatedAt/deletedAt` 유지.
- 영향: 서버·로그인 없음. 동기화(T9)는 나중에 추가 가능.

## D-003 · 2026-09-06 · 천문 계산은 astronomy-engine, 렌더는 Three.js
- 대안: Stellarium Web Engine(AGPL, 임베드 시 라이선스 전염) / 직접 구현(정확도 위험).
- 결정: astronomy-engine(MIT) + 자체 WebGL 렌더러. 별은 J2000 단위벡터 버퍼 + 프레임당 회전행렬 1개.

## D-004 · 2026-09-06 · 데이터 소스
- 결정: HYG v4.4(별), d3-celestial(별자리 선·경계·은하수), OpenNGC(DSO). 모두 CC BY-SA/BSD. Stellarium 데이터·코드는 참고만.
- 영향: 앱 내 라이선스 고지 화면 필요(T0에 골격, T8에 완성).

## D-005 · 2026-09-06 · 망원경 가이드는 "수동 경위대식 push-to" 기본
- 맥락: 마운트 미정(SV48P는 OTA만). 현재는 쌍안경.
- 결정: 폰-경통 부착 + 별 정렬 방식 기본. 적도의(ΔHA/ΔDec)·GoTo(좌표 표시) 모드는 선택지로만. 전동 연동은 보류(D5).

## D-006 · 2026-09-06 · 콘텐츠는 정적 팩으로 선생성
- 결정: GPT Pro로 생성 → Claude 검증 → `public/data/content/v1/*.json` 커밋. 실시간 AI 호출은 보류(D2).
- 영향: 오프라인에서도 스토리 열람 가능, API 키 불필요.

## D-007 · 2026-09-06 · 개발 환경은 경로 A: Claude Code 로컬
- 결정: 데스크톱 앱 Code 탭에서 로컬 폴더(`~/dev/skylog` 등)를 열어 실행. 저장소는 GitHub 원격(push는 사용자 계정의 git 인증 사용). 폰 테스트는 GitHub Pages 배포 URL(T0a) 또는 `vite --host`.
- 모델: 기본 `fable`(Fable 5.1) + effort `high`. 수학·센서·데이터 파이프라인(T0b, T2, T5)은 `xhigh`. 단순 UI·문서·정리 작업은 `sonnet`으로 절약 가능. `max`는 기본 사용 안 함.
- **환경 정보 (2026-09-06, T0a 세션에서 확인)**
  - OS: Windows 11 Education 10.0.26200. 셸: PowerShell 5.1 + Git Bash. 프로젝트 폴더: `C:\Users\JunhyoungPark\OneDrive\Desktop\별관찰해쌀뚜` (Git 저장소 루트, GitHub 저장소 이름은 `skylog`).
  - Node v24.19.0 (winget `OpenJS.NodeJS.LTS`로 T0a에서 설치), npm 11.17.0, pnpm 12.3.4 (`npm i -g pnpm` — `corepack enable`은 Program Files 권한 문제로 실패), git 2.53.0, GitHub CLI 2.100.0 (winget `GitHub.cli`로 설치).
  - gh 로그인 계정: **`JunhyoungPark-NOBEL`** (keyring, https, scopes: repo·workflow·read:org·gist). 저장소 `JunhyoungPark-NOBEL/skylog`(public — 무료 계정 Pages 조건), Pages는 Actions 소스(`build_type=workflow`), URL https://junhyoungpark-nobel.github.io/skylog/.
  - 외부 다운로드: codeberg.org·raw.githubusercontent.com·registry.npmjs.org·svs.gsfc.nasa.gov·api.github.com 모두 접근 가능(T0b `data:fetch` 가능).
  - git 로컬 identity: `Junhyoung Park <jhpark@nobelab.kaist.ac.kr>` (저장소 로컬 설정). 줄바꿈은 `.gitattributes`로 LF 고정, `core.autocrlf=false`.
  - 주의: 프로젝트가 OneDrive 동기화 폴더 안에 있다. `node_modules`·`dist`가 동기화 대상이 되어 느려지거나 파일 잠금이 날 수 있으므로, 문제가 생기면 OneDrive 설정에서 이 폴더를 제외하거나 `C:\dev\skylog`로 옮긴다.

## D-008 · 2026-09-06 · 프로젝트 라이선스 (T0a에서 확정)
- 결정: 코드 MIT(`/LICENSE`, 저작권자 Junhyoung Park), `public/data/` 파생 데이터 팩 CC BY-SA 4.0(`/public/data/LICENSE`, HYG·OpenNGC 파생). 앱 내 "정보/라이선스" 화면은 `docs/DATA-LICENSES.md`를 그대로 렌더하며 Open-Meteo(CC BY 4.0)·7Timer 크레딧을 포함한다.
- 영향: GitHub 저장소는 public(무료 계정 Pages 조건)이어도 문제 없음.

## D-009 · 2026-09-06 · 계획 문서의 정본은 코드 저장소의 `plan/` (Claude Code 로컬 실행 기준)
- 맥락: 실행 도구를 Claude Code(데스크톱 Code 탭)로 확정. Claude Code 세션은 저장소 파일을 직접 읽으므로 저장소가 정본이어야 한다.
- 결정: 저장소 `plan/*`(STATUS·DECISIONS·task 프롬프트·research·reports)가 정본, `CLAUDE.md`가 읽기 순서를 지시. 갱신은 커밋. Claude 프로젝트 문서 사본은 초기 배포본·백업(태스크 종료 시 STATUS만 복사, 선택). 저장소 `docs/`는 코드 문서(ARCHITECTURE·DATA-LICENSES·TESTING·RELEASE).

## D-010 · 2026-09-06 · Dexie DB v1은 Task 0에서 생성, settings는 Dexie가 단일 진실 원천
- 결정: T0에서 10개 테이블(observations, bookmarks, sites, telescopes, eyepieces, binoculars, blobs, progress, settings, cache)을 v1으로 생성. zustand `settingsStore`는 persist storage 어댑터를 Dexie `settings`로 구현(localStorage 사용 안 함). T2(sites)·T3(bookmarks, cache)가 T4 전에 DB를 쓸 수 있다.
- 구현 메모(T0a): persist 어댑터는 스토어 전체를 한 행에 넣지 않고 **키마다 한 행**(`settings.theme`, `settings.lang`, …, `settings.__version`)으로 저장한다 → 다른 모듈·내보내기가 개별 설정을 그대로 읽는다. `sites.isDefault`는 Dexie가 boolean을 인덱싱하지 않으므로 `1 | undefined`로 저장.

## D-011 · 2026-09-06 · 라우팅은 해시 라우터(자체 구현)
- 대안: 탭 상태만(URL 없음) / react-router(history 모드).
- 결정: `src/app/router.ts`의 해시 라우터(`#/sky … #/debug/data`, `useSyncExternalStore`). 이유: GitHub Pages 하위 경로(`/skylog/`)에서 서버 설정 없이 새로고침·뒤로가기가 동작하고, PWA `start_url`이 고정되며, 의존성이 없다(초기 JS 예산).
- 영향: 상세 바텀 시트 등 화면 안 상태는 URL에 넣지 않는다(T3에서 필요하면 쿼리 형태로 확장).

## D-012 · 2026-09-06 · i18n은 i18next + react-i18next
- 결정: `src/i18n/ko.json`·`en.json`을 번들에 포함(오프라인), `initI18n(lang)`으로 부트스트랩, 언어는 `settingsStore.lang`이 원천. 천체 이름은 i18n 키가 아니라 카탈로그 `names.ko/en`(마스터 플랜 §6.6).
- 이유: 보간·복수형·네임스페이스가 표준적이고 React 통합이 안정적. 번들 비용(≈ 15KB gz)은 예산 안.

## D-013 · 2026-09-06 · 툴체인 버전 확정 (T0a)
- Vite 8.2.2(rolldown 기반) + `@vitejs/plugin-react` 6.1.1(플러그인이 rolldown-vite에서는 SWC 대신 자체 플러그인을 권장하므로 `plugin-react-swc` 대신 채택), React 19.2.8, TypeScript **6.0.3**(7.x는 typescript-eslint가 아직 미지원 `<6.1`), Tailwind CSS 4.3.3(`@tailwindcss/vite`, CSS-first `@theme inline`), Vitest 5.0.0(jsdom), Playwright 1.63.0, ESLint 10 + typescript-eslint 8.69, pnpm 12.3.4(`pnpm-workspace.yaml`의 `allowBuilds`로 esbuild 빌드 스크립트 허용), Dexie 4.4.5, zustand 5.0.15, three 0.185.1, astronomy-engine 2.1.19, i18next 26.4.2.
- `tsconfig`는 프로젝트 참조(`tsc -b`: app/node). 경로 별칭 `@/` → `src/`. `noUncheckedIndexedAccess`·`verbatimModuleSyntax` 켬.
- CI(`deploy.yml`)는 typecheck → lint → test → build → Pages. e2e(Playwright)는 로컬 전용(브라우저 설치 시간·CI 비용). 필요해지면 별도 잡으로 추가.

## D-014 · 2026-09-06 · 데이터 팩 v1 포맷·ID 규칙 확정 (T0b)
- 원본: HYG **v4.4**(`hyg_v44.csv.gz`, codeberg Git LFS → `media/` 엔드포인트), OpenNGC `NGC.csv`+`addendum.csv`, d3-celestial 별자리 3종. 은하수 텍스처는 T1에서 결정(NASA SVS milkyway_* 또는 d3-celestial `mw.json`).
- 별 팩: 헤더 16B + 레코드 28B(J2000 단위벡터 f32×3, mag, bv, hip, hygId), 등급 오름차순. bright ≤ 6.5(8,920개, 244KB), deep ≤ 9.0(83,476개, 2.3MB). 인코더/디코더는 `src/catalog/starPackFormat.ts` 한 곳(스크립트·앱 공유).
- `stars-bright.v1.json`(이름/메타 별 3,171개)에 **ra/dec(J2000 도)를 추가**(계획 포맷에 없던 필드) — 팩 없이도 검색·상세·출몰 계산이 좌표를 쓰기 위함.
- DSO id: M > NGC > IC. NGC/IC가 없는 유명 천체는 **`dso:C<n>`(콜드웰)·`dso:B<n>`(바너드)** 허용(`ObjectId` 타입·정규식 확장): C9 동굴성운, C14 이중성단(쌍; NGC869/884는 별칭 C14만), C41 히아데스, C99 석탄자루, B33 말머리.
- **M102 = NGC 5866**(OpenNGC는 M101 중복으로 처리하지만 관행을 따름), M73은 OpenNGC 타입 'Other'지만 메시에라서 포함. OpenNGC `Dup` 행은 마스터의 별칭으로 흡수(C37→NGC6882, C50→NGC2239).
- 포함 규칙: 메시에 110 + 콜드웰 109 + (V ≤ 10 또는 B ≤ 10.8 또는 공통 이름 또는 한글 이름) → 661개. `*`(단일 별)·`NonEx`·`Nova` 제외.
- 검색 정규화: NFC → 소문자 → 공백·하이픈·밑줄·점·따옴표·가운뎃점·괄호 제거. 그리스 문자 별칭은 α/alpha/Alp/알파 4종. 클라이언트도 같은 함수를 써야 한다(T3에서 `src/catalog/search.ts`로 이식).
- 별자리 한글: 한국천문학회 표기 우선(백조자리·헤르쿨레스자리·**작은여우자리**). d3-celestial ko와 다른 항목은 빌드 로그에 표시.
- 기준 표: `tests/fixtures/reference-altaz.json` — JPL Horizons(대전 2026-09-06 21:00 KST, airless) 토성·목성·달·화성. astronomy-engine과 RA/Dec·alt/az ≤ 0.1° 일치 확인.

## D-015 · 2026-09-06 · astronomy-engine 래퍼 경계와 씬 행렬 규약 (T0b)
- `Horizon`/`DefineStar`/`Constellation`은 `src/astro/frames.ts`·`events.ts` 안에서만 호출. 바깥 API는 도(deg)·J2000. `Horizon`의 굴절 인자는 타입상 `string | undefined`이므로 "굴절 없음"은 `undefined`로 넘긴다.
- `eqjToSceneMatrix()`는 기저 벡터를 `RotateVector`로 돌려 열을 만든다(엔진의 행렬 관례에 비의존). 반환은 **column-major Float32Array(9)** → `THREE.Matrix3.fromArray`/GLSL `mat3` 그대로.
- 굴절은 Sæmundsson(참→겉보기)을 CPU(`refraction.ts`)와 GLSL(`render/shaders/refraction.glsl`) 두 곳에 동일하게 둔다. 값을 바꾸면 둘 다.
- 출몰 해석식은 검증·근사 전용. 실제 표시는 astronomy-engine 검색 함수.

## D-016 · 2026-09-07 · G2(한국어 이름 표) 병합 규칙
- 맥락: GPT Pro G2 산출물(`plan/research/`: 별 233행·별자리 88·DSO 66·콜드웰 109·28수 표·검토 메모)이 도착. 콜드웰 NGC/IC 대응은 T0 큐레이션과 **109개 전부 일치**(독립 출처 2개 합치 → 검증 완료로 간주).
- 결정(`scripts/data/merge-g2.ts`, 재실행 가능):
  - 별자리: G2(한국천문학회 용어표, confidence high) 우선 → **허큘리스자리, 여우자리**로 교정(T0의 헤르쿨레스자리·작은여우자리는 폐기). 계절 분류도 G2.
  - 별: 기존 통용 표기(아르크투루스 등)를 표시명으로 유지, G2의 Stellarium 번역 표기(아크투루스 등)는 새 `aliases_ko` 컬럼에 검색 별칭으로. 전통 이름은 유명한 것(직녀성·견우성·북극성·천랑성·노인성)을 우선하고 G2의 28수 역할명(왕랑, 대장 등)은 보조. "…의 구성별"(성군 그룹 관계)은 데이터에 남기되 **검색 별칭에서 제외**. G2에만 있는 120개 별 추가 → 큐레이션 240행.
  - DSO: 기존 붙여쓰기 표기(안드로메다은하) 유지, G2 띄어쓰기·대체 이름은 `alt_names_ko`. 새 id 9개(M77, M102, NGC40, NGC1275, NGC1499, NGC3132, NGC4038/4039, IC2118) 추가.
  - `needs_review`: confidence low 또는 한글 이름 빈칸만 true. 나머지는 편집 검토 완료로 본다(음역 미세 조정은 T8).
- 보류: Naga/HIP64962, Bade(HIP 충돌)는 G2 메모대로 제외. 28수 표는 A7(전통 별자리 오버레이) 때 사용 — Stellarium korean 자료는 GPL v2이므로 **선 연결 데이터는 복사하지 않고** 기준별·이름 표만 참고.

## D-017 · 2026-09-07 · T1 렌더러 결정: 투영·은하수·크기 규칙·라벨 임계
- **투영**: 원근 투영(PerspectiveCamera), 사용자 FOV는 **짧은 변 기준 3°~100°**(`render/projection.ts`). 스테레오그래픽 투영(SHOULD)은 보류 → 110° 대신 상한 100°. 카메라 롤 0, 고도 ±89.5° 클램프.
- **은하수**: NASA SVS 이미지는 직접 파일 요청이 403(Referer·UA 붙여도)이라 포기. **d3-celestial `mw.json`(BSD-3)을 빌드 시 1024×512 등적색 회색조 PNG로 래스터화**(`scripts/data/build-milkyway.ts`, sharp SVG, 5단계 누적 밝기, ±180° 경계 링은 unwrap 후 3회 오프셋). 렌더러는 J2000 방향에서 UV(u = RA/360, v = 0.5 + Dec/180)를 계산하므로 이미지 방향 오류가 없다. 45 KB, 프리캐시.
- **별 크기**: `s0 = 4.5px × pixelRatio × clamp(√(60/FOV), 0.7, 3.5)`, `size = s0·10^(−0.2·(mag+소광))`, 하한 1.2px(그 아래는 알파 감소), 상한 18px. mag < 1.5 가우시안 글로우. B−V → RGB 8점 표(Mitchell Charity 근사). 낮에는 `skyBrightnessPenaltyMag(태양 고도)`를 한계등급에서 빼서 별·행성·DSO·라벨을 숨긴다.
- **행성·태양**: 크기 = max(7px, 등급 기반(상한 18px), 각지름 px). 태양은 등급을 쓰지 않고 max(22px, 각지름×2.2). 달은 구 메시(반지름 = 98·tan(각지름/2)) + 태양 방향 DirectionalLight + 앰비언트 0.08(지구조) → 위상 자동. "확대 표시"는 ×3.
- **레이어 순서(renderOrder)**: 배경 0 → 은하수 10 → 별 20 → DSO 22 → 격자 25·기준선 26 → 별자리 경계 29·선 30 → 행성 35·달 36 → 땅 40·지평선 링 41. 전부 depthTest off, 투명 블렌딩(별·은하수는 Additive).
- **라벨**: HTML div 풀, 최대 60개, 사각형 겹침 회피. 우선순위 선택 > 방위 > 행성 > 고유명 별(FOV 100°→mag 1.5, 60°→2.5, 30°→3.5, 10°→5.0, ≤10° 전부) > 메시에(FOV ≤ 60°) > 별자리 이름(FOV ≥ 90° 12개, ≥45° 16개, 그 외 24개).
- **DSO 표시**: FOV > 60° 메시에 mag ≤ 5만, 30~60° 메시에 전부 + mag ≤ 6.5, 15~30° ≤ 8.5, 6~15° ≤ 10.5, 그 이하 전부. 기호 크기 = 각지름 px, 7~48px.
- **팩 교체**: FOV < 20°에서 `stars-deep` 지연 로드, > 28°에서 bright로 복귀(히스테리시스).
- **hit-test**: 반경 24px, 가중치 = 거리 − 2.5·(6 − mag). 달·행성은 원반 반지름만큼 거리 차감.
- **시간·시점 공유**: `#/sky?t=ISO&alt=&az=&fov=&rate=&select=` 해시 쿼리. 해시 변경 시 재적용. `preserve=1`은 테스트 전용(preserveDrawingBuffer).
- **렌더 루프**: invalidate 패턴. 정지 시 draw 0(HUD로 확인). 실시간은 행렬 1초·행성 250ms 간격, 시간 점프(≥60s)는 즉시.
- **T2 훅**: `CameraController.setOrientationQuaternion(q)` — 씬 프레임 쿼터니언 → alt/az. `viewStore.mode = 'sensor'`일 때 센서 프로바이더가 매 프레임 호출하면 된다.

## D-018 · 2026-09-07 · T2 센서 계층 결정 (G1 리서치 반영, 실기기 검증 전)
- **Provider 우선순위**(G1 §A7-1): `deviceorientationabsolute`(Android, 자북) → `AbsoluteOrientationSensor`(Generic Sensor, 선택적) → `deviceorientation`(absolute=true면 자북, 아니면 iOS 상대 + `webkitCompassHeading`). 1.5초 안에 유효 샘플이 없으면 다음 Provider. 데스크톱은 시뮬레이터(설정 → 개발자 → 센서 디버그). 권한 요청은 "폰으로 하늘 보기" 버튼 탭 안에서만(`requestPermission` 기능 감지, iOS 식별 아님).
- **자세 변환**(`sensors/orientation/math.ts`): `q_scene = qY(α)·qX(β)·qZ(−γ)·qX(−π/2)·qZ(−θ)` — Three.js 구 DeviceOrientationControls와 동일, G1이 `S·Rz(α)Rx(β)Ry(γ)Rz(−θ)`와 동치임을 검산. 카메라 = 로컬 −Z(후면 카메라). α는 위에서 볼 때 반시계(heading = 360 − α). 방위 = `atan2(p_x, −p_z)`, **+Y 양의 회전은 방위를 줄이므로** `yawQuaternion(Δaz) = qY(−Δaz)`. 8개 테스트 벡터 × 화면 회전 4개 통과.
- **편각**: WMM2025(`magvar` 2.2.0, MIT). 대전 2026-09 = **−8.70°**(서편각). 진북 방위 = 자북 방위 + D. 자북 소스(Android 절대, **iOS `webkitCompassHeading`도 자북** — WebKit `WebCoreMotionManager`가 `magneticHeading`을 전달한다는 G1 확인)에 한 번만 적용. 별 정렬 δ가 있으면 정렬이 편각을 흡수(재적용 금지). 설정에서 끌 수 있고 디버그 패널에 값 표시. task-02 초안의 "iOS 기본 미적용"은 G1 근거로 **적용**으로 바꿈 — 실기기 3-자세 프로토콜로 확정.
- **iOS yaw 동기화**(`compassSyncCandidate`): 상단 축(+Y) 기준(`compassAxis='top'`, 미검증 상태), |β| ≤ 60°·|γ| ≤ 45°·투영 길이 ≥ 0.5·accuracy 0~15°·정지(각속도 < 10°/s)일 때만 δ 후보 = (heading + D) − 상단축 상대 방위. 초기값 5샘플 원형 평균, 이후 τ=3s 지수 평활, 15°/s 이상 튀는 값 무시. 세운 자세(β≈90°)·젖힌 자세에서는 갱신 중단(180° 반전 방지).
- **필터**(`OrientationFilter`): slerp 저역통과 τ=100ms, 각속도는 100ms 창 변위로 측정(노이즈가 30°/s처럼 보이는 것 방지), > 30°/s면 이득 1, 출력 데드밴드 0.2°(추정치는 항상 갱신 — 첫 샘플 편향 고정 방지), 절대 소스는 yaw τ=500ms 별도 평활. 이상 감지: 한 샘플(≤120ms) 사이 yaw ≥ 15° & pitch 변화 < 5°. 단위 테스트: 정지 잔여 < 0.2°, 90° 스텝 2° 이내 ≤ 150ms.
- **보정 모델**: `q_world = R_yaw(δ)·q_sensor` + 피치 오프셋(카메라 오른쪽 축). `solveYawOffset(samples[])`는 여러 별의 원형 평균·잔차를 내므로 T5 폰-경통 정렬로 확장 가능. 보정 마법사 3단계, 드래그 미세 조정, 저장된 보정은 같은 관측지 이름이면 재사용.
- **AR UX**: 수동 드래그 → 5초 일시 정지("수동") 후 자동 복귀, "센서 복귀" 버튼. 롤은 기본 반영(설정 "수평 유지"로 무시). 진동은 Android만, 시각 플래시가 1차 피드백, 소리는 옵션.
- **위치**: `getCurrentPosition`(12초) → 정확도 > 100m면 `watchPosition`으로 30초까지 개선. 마지막 위치는 Dexie `settings('sensor.lastFix')`. 시작 시 기본 관측지 > 마지막 GPS > 대전 프리셋. `coords.heading`은 쓰지 않는다(이동 방향).
- **관측지 범위(C15)**: `Site.visibleAz = [[start, end]]`(단일 구간, 시계 방향), `minAltDeg`. T3 추천의 하드 필터. 하늘 화면의 범위 밖 어둡게 표시는 보류.
- **미검증(실기기 필요)**: iOS heading 축·자세별 동작, Android 편각 부호 체감, 실제 지연·떨림, 화면 회전. 결과 반영 후 `task-2-done`.
- **2026-09-07 추기**: 사용자 실기기 테스트 통과("매우 괜찮음, 문제 없이 잘 됨"). 덤프·기기 정보 없이 통과 보고만 있어 위 기본값을 그대로 확정하고 `task-2-done` 태그. iOS 축·편각 가설은 문제 보고가 있을 때 덤프로 재검토.

## D-019 · 2026-09-07 · T3a 결정: 검색 엔진·상세 계산·찾아가기·관측 밤
- **검색 라이브러리 없음**: MiniSearch/Fuse 대신 `search-index.v1.json`(3,929 항목·약 3만 정규화 별칭) 선형 탐색. 질의당 0.5~3ms(수용 기준 50ms). 이유: 인덱스가 이미 정규화되어 있고 한글·기호 규칙을 우리 함수로 통제해야 하며(빌드 `scripts/data/lib.ts`가 `src/catalog/normalize.ts`를 재export), 랭킹(등급·밝기·지평선·유명 천체)을 직접 정의하는 편이 짧다.
- **랭킹 규칙**: exact(0) < prefix(100) < substring(200) < fuzzy(300, 편집 거리 1·4자 이상) + 등급(mag) − 유명 천체 5 + 지평선 아래 20. 별자리 "…자리" 뗀 별칭과 한국어 음역 확장(`koreanLatin.ts`)은 **약한 일치**(정확 일치라도 prefix 등급) — "안드로메다"는 M31, "오리온자리"는 별자리가 1위. 초성 질의는 prefix 등급.
- **가장자리 화살표**: 카메라 공간 x·y를 직접 써서 방향을 정한다(원근 나눗셈이 없으므로 task-03 §4의 "뒤쪽 뒤집기"는 적용하지 않음 — 적용하면 뒤·오른쪽 목표가 왼쪽을 가리킨다). e2e에서 ±90° 방위 오프셋으로 검증.
- **상세 시트 계산**: "오늘"은 정오→정오(`nightKey`) 안의 사건만 표시(밖이면 —). 최적 시간대 = 어두운 구간(천문박명 사이, 없으면 항해박명·일몰~일출) ∩ 고도 ≥ 30°(없으면 20°) 최장 연속(10분 샘플, 최소 20분). 가장 좋은 달 = 매달 15일 현지 자정 LST와 적경 차 최소.
- **장비 판정(근사, UI에 "참고" 표기)**: 점광원은 한계등급 여유(맨눈 = Bortle NELM − 달 − 소광, 광학계 = 2+5log D − Bortle 4 대비 손실 절반 − 달 − 소광). 확산 천체는 표면 밝기(mag + 2.5log 면적) vs Bortle 하늘 배경(22.0…18.0 mag/arcsec²) 대비 + 쌍안경 +1.0 / 망원경 +2.0 보너스, 총 등급도 한계 안이어야 함. 기본 장비: 쌍안경 10×50, 망원경 90mm f/5.5(T5가 프로필로 대체). Bortle은 관측지(`Site.bortle`) 없으면 7.
- **어두운 창**: 천문박명 사이에서 달 고도 < 10°(굴절 포함) 구간. 5분 샘플 + 이분법 3회(≈1분). 달 나이 = 위상각/360 × 29.530588일. 독립 기준은 USNO rstt API(대전, 일몰·시민박명·월출몰 모두 ±2분 통과; timeanddate는 403이라 사용 안 함).
- **북마크(☆)**: Dexie `bookmarks`에 소프트 삭제(deletedAt)로 토글, 되살리기는 같은 행 재사용. T4가 목록·통계 UI를 확장.
- **씬 준비 대기**: `SkyScene.ready`는 카탈로그 로드 + 첫 행성 배치 후 resolve. 캐시된 데이터로 즉시 로드되는 재마운트에서 `flyToObject`가 첫 프레임 전에 호출되어 실패하던 문제의 원인.
- **보류**: 상세 시트 파인더 차트(SHOULD), 검색 Worker 이동, 어두운 창의 구름 반영(T3b), "실제 하늘처럼"(T3b).

## D-020 · 2026-09-07 · T3b 결정: 날씨·추천 엔진·천문 현상·실제 하늘처럼
- **날씨**: Open-Meteo만(키 없음, CORS 허용, CC BY 4.0 표기). 1시간 캐시는 Dexie `cache`. **7Timer ASTRO는 생략** — `astro.php` 응답에 `Access-Control-Allow-Origin`이 없어(curl로 확인) 브라우저에서 호출 불가. 프록시는 만들지 않는다(task-03 §9). 시상·투명도는 표시하지 않음.
- **추천 엔진**(`astro/recommend.ts`, 순수 함수): 10분 샘플마다 회전행렬 1개로 모든 고정 후보를 변환. 가시 조건 = 종류별 최소 고도(DSO 20·별자리 15·별 10·행성 8·달 5) ∧ 관측지 `visibleAz`/`minAltDeg` ∧ 구름 < 70% ∧ 종류별 어둠(태양 고도: DSO −12°, 별자리 −9°, 별 −6°, 행성 −3°, 달 0°). 안 보이는 이유(horizon/site/twilight/cloud)를 기록해 "곧 진다/올라오는 중"은 지평선·범위 때문일 때만. 최적 시각 = 고도 항 − 달 항의 최대 샘플. 점수 = 고도 30·(≥30° 지속) 20·달 −7×페널티(점광원 절반)·장비 판정 25/15/4·달·고전 5행성 +30(천왕·해왕 +8, 안 보이면 30%)·이벤트 +20·계절 +8·유명 +5·이중성 +6·신선도 +5. 점수용 판정은 달 제외(달은 M 항에서 한 번만), 그룹 배치용 판정은 달 포함. 그룹은 "가장 잘 맞는 장비" 규칙(맨눈 잘 보임이면 쌍안경 그룹 제외, 쇼피스 예외; 행성은 망원경 항상, 쌍안경은 달·목성·천왕·해왕만). 지금 당장 ≥ 35점·최대 8(별자리 2), 올라오는 중 ≥ 50점·최대 6. 계절 시그니처의 "체감 달"은 창 중앙 시각으로 보정(21시 +0, 01시 +2, 04시 +4개월).
- **장비 판정 보정**: 큰 산개성단(장축 ≥ 30′)은 적분 등급 + 1.5를 점광원처럼 판정(M45 잘 보임 / M44 어려움 at Bortle 7). 은하·구상성단은 중심부 보정 +1.5(면적 1/4).
- **천문 현상**(`astro/phenomena.ts`): 행성 충·합(`SearchRelativeLongitude` 0/180), 내행성 최대이각(시민박명 때 고도 ≥ 8°여야 "보임" — 2026-10-12 수성은 25°이각인데 9° 고도라 안 보임), 금성 최대 밝기(`SearchPeakMagnitude`), 달 위상 4분기, 월식(극대 달 고도 > 0)·일식(`SearchLocalSolarEclipse` ±1일 매칭), **슈퍼문은 Espenak 규칙**(망 거리 ≤ 근지점 + 10%×(원지점−근지점); ±1일 규칙은 2026-11-24를 놓침), 유성우 극대 밤 등급 = 달 조도 × (1 − 어두운 창 비율) → good ≤ 0.15, fair ≤ 0.45. 추천에는 충(±30일)·최대이각(±10일)·슈퍼문(±1일)·유성우 극대(복사점 별자리, ±2일) 보너스.
- **실제 하늘처럼**: 한계등급 = Bortle NELM − 달 페널티(박명은 렌더러가 태양 고도로 따로 뺌, 구름 미반영). Bortle은 레이어 설정 > 관측지 > 7.
- **오늘 밤 창**: 저녁 프리셋은 일몰부터(금성·초승달·목성이 박명 중에 보인다; DSO는 −12° 게이트가 막는다).
- **ObjectId 확장**: 데이터 팩 v1에 실재하는 `dso:Mel111`·`dso:Cr399`·ESO/PGC/UGC/HCG/MWSC/H 계열을 타입·정규식에 허용(팩 포맷 변경 없음, D-014 보완).
- 보류: 추천 Worker 이동, Krisciunas & Schaefer 달 모델, 파인더 차트.

## D-021 · 2026-09-07 · UI 리프레시(둥근 표면·유리 내비게이션 층)와 한국어/영어 카피 규칙
- 맥락: 사용자가 "딱딱하고 각진" UI를 요즘 유행(둥근 모서리)으로 바꾸길 원함. 리서치(iOS 26 Liquid Glass·Material 3 Expressive·NN/g·KRDS)와 코드 진단: 원인은 불투명 1px 외곽선·하드 디바이더·작은 반지름·모션 부재·uppercase 헤더.
- **토큰 v2**(`src/app/theme.css`): 반지름 xs8/sm12/md16/lg20/xl24/2xl28/3xl32/4xl40/pill(Tailwind 기본 스케일 덮어씀), 표면 층 bg→surface→surface-2→surface-3(야간 #120303/#1c0605/#260807), fg 알파 헤어라인(야간에 자동 붉음), 톤 상태색(accent-soft/success-soft/danger-soft), 그림자는 상단 1px 하이라이트 + 옅은 앰비언트, 유리(`glass`/`glass-strong`/`glass-sm`/`glass-off`, `@supports` 폴백, `prefers-reduced-transparency`·`html[data-glass='off']`), 스프링 이징(M3 Expressive ζ0.8/k380·ζ0.6/k800을 `linear()`로 샘플), 한글 타이포 스케일(본문 16/24), 야간 `--muted` #7a1010(1.9:1) → #d9503f(5.2:1).
- **구조**: 상태 바는 떠 있는 HUD 캡슐, 탭 바는 떠 있는 pill(활성 탭 accent-soft 인디케이터), 바텀 시트 28px 유리(하늘 위·반쯤 열림일 때만, 그 외 불투명), 하늘 컨트롤은 유리 캡슐, 레이어 패널·리스트·카드는 불투명. 유리 위 텍스트는 fg. 콘텐츠 화면은 인셋 그룹 카드(24px).
- **카피 규칙**(ko): 라벨은 짧은 명사형, 문장은 해요체(합쇼체 제거). 용어집: 천문박명 끝 → "완전히 어두워짐", 시작 → "밝아지기 시작", 항해박명 → "거의 어두워짐", 박명 → "어스름", 남중 → "가장 높을 때", 주극성 → "하루 종일 떠 있음", 각거리 → "…와 떨어진 각도", 각지름 → "보이는 크기", 조도 → "밝은 부분 %", 달 나이 → "월령", 상현망간/하현망간 → "차오르는 달/기우는 달", 한계등급 → "{{mag}}등급까지 보임", Bortle → "광해 N단계", 편각 → "나침반 북쪽 보정(편각)", 어두운 창 → "가장 어두운 시간", 별로 정렬 → "별에 맞추기", 시간 여행 → "시간 이동 중". 유지: 고도·방위·등급·일출/일몰/월출/월몰·관측지·천구 적도·황도·자오선·J2000. en은 sentence case·미국식 철자, "First light" 같은 은어 금지("Dark ends"), 편각 토글과 무관하게 참인 "Compass heading".
- 영향: e2e가 검사하는 문자열(보정됨·잘 보임·★ 예정됨·그믐달·삭제·복사됨·고도)은 유지. `Sites`의 Bortle 셀렉트는 i18n 키(`sites.bortle*`)로. 실기기 체크리스트에 "달이 캡슐 뒤에 있을 때 대비"·"야간 모드×유리 on/off" 추가.
- **추기(스크롤, D-022로 이관)**: `scroll-fade-y`(스크롤러 자체의 `mask-image`)는 폐기 — 아래쪽 페이드는 `ui/ScrollArea.tsx`의 오버레이가 그린다.
- **추기(재스타일 검토 반영)**: (1) `theme.css`의 베이스 리셋(`html/body`, `button{background:none;padding:0}`, `:focus-visible`)은 **반드시 `@layer base` 안에** — 레이어 밖 규칙은 `@layer utilities`를 이기므로 `bg-accent`·`px-*`가 전부 무시된다(캡슐 버튼이 투명하게 렌더된 원인). (2) `<main>`은 `isolate`로 스태킹 컨텍스트를 만들어 하늘 뷰 내부 z-index가 떠 있는 크롬(z-20)과 경쟁하지 않게 한다. (3) 시트 전체 높이는 `calc(100% − status − safe-area-top − 8px)`; 탭 pill 하단 여백은 `calc(--tab-inset + safe-area-bottom)`으로 `pb-tab`/`bottom-sky`와 같은 식. (4) 유리 유틸리티에는 그림자를 넣지 않고 `glass shadow-float`처럼 조합. (5) 보정 마법사 루트는 `pointer-events-none`(카드만 auto)이어야 캔버스 드래그 미세 조정이 동작한다. (6) 시트가 열리면 하늘 뷰 하단 독은 `invisible`(유리 위 유리 방지, 훅은 유지). (7) 유리 위 텍스트는 `text-fg`(muted는 대비 실패).

## D-022 · 2026-09-07 · 스크롤: 마우스 드래그 스크롤(관성) + 스크롤러 mask 폐기 + 얇은 스크롤바
- 맥락: 사용자 보고 "스크롤이 부드럽지 않고, 스크롤 바를 정확히 눌러야 스크롤된다 — 근처를 눌러도 위아래로 스크롤되게". 진단: (1) 마우스 환경(데스크톱 브라우저·Claude 앱 브라우저 창)에서는 브라우저가 콘텐츠 드래그로 스크롤하지 않는다(휠·스크롤바만). 터치 경로를 막는 요소는 없었다(`touch-action: none`은 캔버스·범위 링·시트 손잡이뿐). (2) 탭 화면·레이어 패널 스크롤러에 걸린 `mask-image`(D-021 `scroll-fade-y`)가 합성기 스크롤 최적화를 깨 폰에서 끊김을 만든다.
- **`ui/useDragScroll.ts` / `ui/ScrollArea.tsx`**: 세로 스크롤 영역은 `ScrollArea`(탭 화면 `TabScreen`·상세 시트 본문·설정 `ScreenFrame`·레이어 패널)로 통일하고, 높이 제한이 있는 곳(보정 마법사 후보 목록)은 훅만 붙인다. 규칙: `pointerType === 'mouse'`만 처리(터치·펜은 네이티브), 6px 임계값 뒤 첫 이동 방향으로 축 고정(가로면 가장 가까운 `overflow-x` 스크롤러 — 칩 행), 드래그 중 `user-select: none`·`cursor: grabbing`·`data-drag-scrolling="1"`, 놓으면 최근 100ms 속도로 관성(지수 감쇠 τ=325ms, 최대 4px/ms, 경계에서 정지), 휠·새 pointerdown이면 즉시 정지, 드래그 직후 400ms 안의 `click`은 캡처 단계에서 삼킨다(끌다 놓은 버튼이 눌리지 않게), 입력 요소·`[data-drag-scroll="off"]`·`touch-action: none` 조상에서 시작한 드래그는 무시(범위 링·시트 손잡이 보호).
- **페이드**: 스크롤러에 `mask-image`를 걸지 않는다. `ScrollArea fadeBottom`이 `relative` 래퍼 + `pointer-events-none` 그라디언트 오버레이(기본 `--bg`, 레이어 패널은 `--surface`)로 같은 28px 페이드를 그린다. 스크롤러에는 `overscroll-y-contain`(스크롤 체이닝 방지).
- **스크롤바**: `@layer base`에 `* { scrollbar-width: thin }` + `html { scrollbar-color: fg 24% / transparent }` — 얇고 야간 모드에서 붉어진다. 칩 행의 `[scrollbar-width:none]`은 utilities 레이어라 그대로 이긴다.
- 검증: 단위(`tests/unit/ui/useDragScroll.test.ts` — 임계값·클릭 억제·터치 무시·입력/off 영역·가로 축·관성·순수 함수) + e2e(`tests/e2e/scroll.spec.ts` — 마우스 드래그·관성·버튼 위 드래그 억제·CDP 터치 네이티브 스크롤·시트 본문).
- 보류: 스크롤 위치 복원(탭 전환 시), `scroll-snap`은 쓰지 않음. 유리(backdrop-filter)가 폰에서 무겁다고 느껴지면 설정 → 반투명 끄기(이미 있음).

## D-023 · 2026-09-07 · 관측 기록·DB v2·백업
- 사용자 요청에 따라 기록 탭(T4)을 콘텐츠·학습 통합과 함께 구현한다. DB는 v2로 올리되 10개 테이블과 인덱스를 유지한다. 기존 기록의 notes/tags/nightKey 누락만 채우며 v1→v2 보존 테스트를 둔다. 공개 카탈로그 팩 포맷은 그대로다.
- 한 관측 밤은 기존 정오→정오 nightKey를 쓴다. 별도 세션 엔티티는 도입하지 않는다. 관측 결과(seen/notSeen/unsure)와 예정 북마크를 구분하고, 하늘·검색·추천에 동일한 파생 집합을 연결한다.
- 기록은 장비·시각·위치·달·고도·날씨를 스냅샷으로 보관한다. 스케치는 PNG, 사진은 긴 변 최대 1600px로 줄여 별도 blobs 테이블에 저장한다. 삭제는 소프트 삭제와 실행 취소를 제공한다.
- JSON 백업은 기록·사진/스케치·학습 진도·설정·장비를 포함한다. 같은 ID는 최신 우선 또는 모두 추가(참조 ID 재매핑)를 선택하며 한 트랜잭션으로 가져온다. CSV는 BOM 포함, 좌표 반올림은 선택 사항이다.
- 기록이 있고 백업이 없거나 30일이 지나면 알림, 나중에는 7일 숨김. 백업 시각/학습의 backup 이벤트는 브라우저에 파일 다운로드를 요청한 시점이다. 실제 파일 보관과 다른 기기 복원은 사용자가 확인한다.
- Windows 테스트에서 jsdom Blob을 Node structuredClone이 보존하지 못한 문제는 테스트 환경에 Node Blob을 사용해 해결했다. 실브라우저 IndexedDB·다운로드·별도 브라우저 복원 테스트로 실제 데이터 왕복도 확인했다.

## D-024 · 2026-09-07 · G3 이야기 121개와 자연스러운 본문
- 통합팩의 G3-content-cumulative-121.json만 정본으로 삼는다. 이전 배치를 다시 합치지 않는다. 원본은 plan/research/G3-G5-integrated/와 data-src/content-raw/G3-original/, 재서술본은 G3-natural/에 따로 보존한다.
- 자연어 문장은 해요체로 풀고, 입력 파일명·작업 지시 같은 표현은 사용자 본문에서 뺀다. 전설과 관측 사실을 구분하고 출처·불확실성을 유지한다. 항목 버전은 원본 1→재서술 2, 새 팩은 content/v1이다.
- 빌드와 앱이 같은 TypeScript 스키마 검사를 사용한다. 추가 스키마 의존성은 넣지 않는다. 필수 출처·태양 안전 문구·길이·ID를 검사하고, 수치는 카탈로그와 대조한다(등급 ±0.3, 거리 별 ±30%/DSO ±40%, 각크기 ±30%, 분광형 앞 두 글자). 충돌은 검토 메모로 남기며 카탈로그를 근거 없이 고치지 않는다.
- 미확정/미확인/검토 보류 값은 사실표에서 제외하고, 외부 소개값·대역 미확인 값은 검토 배지를 붙인다. **121개 포함·제외 0·needsReview 112개(메모 418개)**. Capella 분광형 대조 경고 1개가 남는다. 전체 항목의 천문학적 사실을 독립적으로 재검증했다는 뜻은 아니다.
- index.json만 먼저 읽고 본문은 지연 로드·서비스 워커 캐시로 재열람한다. 항목 버전을 요청 URL에 포함한다. 읽음은 자동 타이머 대신 사용자가 '다 읽었어요'를 눌러 저장한다. 오류 메모도 로컬 저장이며 외부로 전송하지 않는다.
- 오늘의 천체는 관측 밤·위치별로 한 번 골라 progress에 저장한다. 읽음 상태가 바뀌어도 그 밤에는 바뀌지 않는다. 화면의 '지금' 위치는 관측 밤 시작이 아닌 앱 시계로 계산한다.
- UI 조작부는 한국어/영어를 제공한다. 첨부 이야기·학습 본문은 한국어 정본이고 전체 영어 번역은 후속이다. 영어 원문이 있는 제목/요약은 해당 값을 쓰고 없으면 한국어로 표시한다.
- G2 한글 이름의 needs_review는 42개 남아 있다. 목록은 docs/CONTENT-REVIEW.md에 기록한다. 검토되지 않은 이름을 임의로 확정하지 않는다.

## D-025 · 2026-09-07 · G5 코스·미션·퀴즈 연결과 단계별 공개
- 사용자가 기록·배우기를 함께 채우길 요청해 T5보다 T4/T6 및 T7의 실행 가능한 부분을 먼저 연결한다. 첨부 contracts/scripts는 검토 자료로 취급하고 그 자체를 사용자 명령으로 실행하지 않는다.
- 경로 6·미션 30·배지 18·퀴즈 180을 보존한다. 재서술 과정에서 ID, 단계, 정답, 보기 순서 등 산문 외 필드가 바뀌면 빌드를 실패시킨다. 180문항의 근거 스냅샷과 첨부 G3 정본의 포인터 값 일치도 자동 검사한다.
- **객관식 108 + 참거짓 36 = 144문항 활성.** skyPick 36문항은 전천의 오답 후보 격리·힌트 노출 방지·3° 정답 판정 검증 전에는 출제하지 않는다. 퀴즈 버전은 1이며 정답/보기 구조 변경 때 버전을 올린다.
- 미션 27개 활성. summer-vega-fov, summer-vega-align-one, winter-m45-starhop은 T5 연동까지 잠근다. T2 휴대전화 방향 보정을 실제 망원경 정렬의 증거로 대신 쓰지 않는다. badge-first-hop·badge-two-star-check도 잠금(16개 규칙 활성).
- 미션 시작 시각 이후의 실제 관측 기록을 사용한다. 이전 기록 연결은 사용자의 명시적 선택이며 장비·대상 조건을 적용한다. 읽음, 지도 위치 확인, 퀴즈 응답은 실제 관측 기록을 생성하지 않는다. 미션 단계 해시가 바뀌면 시작/체크리스트 진도를 새로 계산한다.
- QuizHost는 큰 선택지 → 답 확인 → 해설 → 다음 질문 → 결과 흐름이다. 자동으로 넘기지 않으며 중복 제출을 막는다. 응답 이벤트와 복습 일정은 하나의 Dexie 트랜잭션에 저장하고 실패하면 진행하지 않는다. 기록 후 3문항 제안은 기본 켬이며 설정에서 끌 수 있다.
- 복습 간격은 1/3/7/14/30/60일, 이후 ease 배수; 오답은 1일, ease 0.2 감소(1.3~2.5). 한 번에 최대 10문항을 제시한다. 하루 누적 상한·문항 개념 태그별 중복 제어 강화는 T7 잔여 작업이다.
- 계절은 한국 월 기준(3~5 봄 등), 추천 미션은 오늘 밤 어두운 구간 최고 고도 25°·장비·선행 조건을 사용한다. 어두운 관측지 요구는 시작 전 확인한다. 구름·주변 장애물은 추천 문구에서 현장 확인을 안내한다.
- 배지는 현재 기록에서 파생해 표시한다. 별자리 배지는 별자리 자체의 실제 관측 기록만 센다. 삭제·복원 뒤 재계산한다. 영구 획득 이력/축하 연출은 후속이며 T7 완료 태그는 붙이지 않는다.

## D-026 · 2026-09-07 · 배우기 탐색 분리와 주제별 퀴즈 여정
- 사용자 최신 요청: 복잡한 배우기 화면 분리, 테마/난이도별 도장깨기, 향후 리더보드. 하단 탭 추가는 제안이었으므로 5개 주요 목적지는 유지하고 배우기 안에 퀴즈·코스·이야기·업적을 두기로 결정했다. 상단 메뉴는 가로 넘침 없이 44px 이상, 위치·센서 상태 캡슐을 학습 화면에서 제거한다.
- 첫 화면은 다음 스테이지 하나를 강조한다. 코스 추천은 3개까지만 코스 화면에서 제안하고 6개 전체 코스를 탐색할 수 있다. 121편 이야기에는 제목/ID 검색, 종류/안 읽음 필터를 제공한다. 업적은 획득 조건을 짧게 쓰고 모은 업적 필터·관측 통계 펼치기를 둔다.
- section/path/mission/chapter를 해시에 둬 새로고침·브라우저 뒤로 가기를 지원한다. 하단 탭으로 돌아와도 마지막 배우기 화면을 유지한다. 퀴즈는 배경 inert, 포커스 복귀/순환, 선택지 방향키, 메뉴 방향키/Home/End를 제공한다.
- 브라우저의 user-scalable=no를 제거해 읽기 화면 확대를 허용한다. 하늘 캔버스의 touch-action:none은 유지해 핀치로 천구 FOV를 조절하는 동작과 구분한다.
- **stageCatalog.json**: 현재 활성 객관식/참거짓 144문항을 한 번씩만 고정 배치한 28스테이지. 첫 별빛(기초1) → 관측자의 눈(응용2) → 우주의 탐정(심화3). 챕터마다 달/행성·별/별자리·성단/성운/은하 테마, 단계당 2~6문항. stage ID/version=1 및 question ID/version을 고정한다. 구성/보기 변경 시 해당 버전을 올려야 한다. 기존 v1 데이터 팩 형식은 변경하지 않는다.
- 정확도 60%/80%/100%에서 별 1/2/3개. 80% 이상이면 도장을 받고 바로 다음 스테이지 해제. 시간 제한/시간 가점 없음. 점수는 round(정답수/전체수×1000), 개인 합계는 단계별 최고점만 더한다. 연습/복습/기존 퀴즈 응답은 미션·복습에 유지하되 새 스테이지 도장을 소급 생성하지 않는다.
- progress의 learn.stage-draft:<runId>에 응답을 순서대로 기록하고 마지막 응답·SR·learn.stage:<runId> 완료를 단일 트랜잭션으로 확정한다. runId+index 재전송은 멱등, 실패 시 마지막 응답/완료/SR 모두 롤백. 미완료 종료는 도장을 주지 않으며 새 도전은 첫 문제부터 시작한다. 완료 이력은 보존하고 버전/전체 응답에서 최고점을 재계산한다. 일반 JSON 백업/복원에 포함, DB v2 인덱스 변경 없음.
- 리더보드는 **향후 기능**이다. 현재는 개인 로컬 점수만 저장한다. 외부 경쟁에는 계정/동기화·문항/단계 버전·서버 채점·중복/위변조 방지·시즌 규칙을 별도로 설계해야 하며 로컬 점수를 공인 순위로 사용하지 않는다.
- 참고(2026-09-07 열람): [Apple HIG Tab bars](https://developer.apple.com/design/human-interface-guidelines/tab-bars?changes=l_1__4&language=objc)의 탭 수/복잡성 균형·숨겨진 탭 회피·상태 보존, [Duolingo 학습 경로 설명](https://blog.duolingo.com/new-duolingo-home-screen-design/)의 작은 학습 단위·점진적 경로·복습(2022 사례), [WCAG 2.2](https://www.w3.org/WAI/standards-guidelines/wcag/new-in-22/)의 가려지지 않는 초점·입력 목표. 시각적 모방보다 탐색/진도 구조를 참고했다.

## D-027 · 2026-09-07 · 기본 반투명 지면과 지평선 아래 천체
- 사용자 요청에 따라 ground opacity를 0.85→0.28, 새 showBelowHorizon 기본 true. 기존 설정은 덮어쓰지 않으며 ground && groundOpaque일 때 불투명 지면이 투시보다 우선한다.
- 별/별자리 선·경계/DSO/행성/달을 지평선 아래에도 그린다. 별의 지하 대기 소광은 -6°까지 부드럽게 해제해 보이지 않던 별을 표시한다. 지평선 아래 라벨은 0.6 배 밝기로 낮추고, 화면 중심이 아래면 실제 시야에서 가려진다는 짧은 안내를 둔다.
- 숨김/불투명 모드에서는 그리기·라벨·선택·기록 마커를 함께 숨긴다. 태양/행성의 크기 0인 스프라이트는 fragment를 버려 숨긴 점이 1px로 남지 않게 한다.
- astro/visibility·추천·미션 관측 조건·장애물 판정은 그대로다. 투시는 지도 탐색용이며 실제 관측 가능을 뜻하지 않는다. 데이터 팩/좌표계/RA 규약은 바뀌지 않는다.

## D-028 · 2026-09-08 · 관측·망원경 퀴즈와 오늘 밤의 정보 분리

- 사용자 요청으로 `observing-quiz.json`에 독자 작성한 한·영 60문항을 추가한다. G5 정본/자연어본 180문항은 덮어쓰지 않는다. 구조 대조는 G5에 적용하고 추가 문항은 동일 스키마·참조 검증을 거친다. 문항 출처/검토는 `docs/OBSERVING-CONTENT.md`.
- 기존 하늘 코스 28스테이지/144문항 ID·version·문항 순서는 그대로다. 관측·망원경 코스는 별도 `observingStages.json`의 12스테이지(3챕터, 단계별 5문항). 각 코스 첫 단계부터 시작하고 해제·최고점은 코스별로 계산한다. 새 코스 때문에 기존 마지막 단계의 다음 단계가 바뀌지 않는다.
- 총 240문항 중 204 활성, skyPick 36은 계속 잠금. 기존 DB v2·learn/v1 팩 구조는 유지한다. 문항과 단계 version=1, 이후 내용/구성 변경 시 해당 버전을 올린다. 온라인 순위는 이번 범위에 포함되지 않는다.
- 오늘 밤은 추천 / 관측 조건 / 천문 일정으로 분리한다. 추천을 기본 화면으로, 시간·장비는 요약+변경 버튼, 하이라이트와 시간순 계획은 접기/펼치기. 기존 날씨·추천·이벤트 계산을 유지하며 Tab/방향키/Home/End 탐색을 지원한다.

## D-029 · 2026-09-08 · 별길 가이드: 휴대폰 윗변·상대 센서·실제 시야

- 이름은 **별길 가이드 / Starpath guide**. 상용 StarSense의 이름/코드/데이터를 사용하지 않는다. 이 구현은 밝은 별에 직접 정렬하는 센서 push-to이며, 영상 plate solving은 T5의 범위 밖이다. 앱 진입 안내에도 이 차이를 명시한다. 제조사 기능 설명은 `docs/OBSERVING-CONTENT.md`.
- 사용자 요구대로 후면 카메라(-Z)가 아닌 물리 **+Y(폰 윗변)**를 경통과 평행하게 놓는다. 화면 좌표 회전을 제외하고 `RelativeOrientationSensor`(device frame) 우선, `deviceorientation`의 absolute=false만 대체로 쓴다. 나침반/AR 보정은 섞지 않는다. 센서 없는 기기는 차트/스타호핑을 사용할 수 있다.
- 모델은 `qY(-yaw) × q_relative × axis`. 1별은 물리 +Y 기준 방위 편차로 시작. 2별은 서로 다른 별, 20~150° 이격을 요구하고 초기 yaw±40°에서 장착축과 yaw를 최소제곱 추정한다. 잔차 최대 2°, 장착축의 +Y 이탈 25° 초과·해 구분 부족은 거부한다. 낮은 두 점 잔차가 다른 방향의 정확도를 보장하지 않아 세 번째 별 확인을 제공한다.
- 정렬 시각의 별 방향을 고정하고 목표는 현재 시각으로 계산한다. 실제 가이드는 실시간, T2 시뮬레이터만 시간 이동 상태를 쓴다. 센서 데이터 1.5초 중단·프로바이더 교체·앱 재진입·장비 변경 시 재정렬한다. 저장된 정렬은 참고 이력이며 상대 센서 재시작에 재사용하지 않는다. 10분 후 드리프트 알림. 목표 시야 진입은 테마 플래시·지원 기기 진동·설정된 소리로 알린다.
- 장비 프로필은 기존 telescopes/eyepieces/binoculars CRUD를 사용한다. Telescope에 광학 방식·천정미러·상 방향·회전·정립 파인더 옵션을 선택 필드로 추가(인덱스/DB/백업 버전 변경 없음). 예시는 보유 장비 가정이 아니다. 저장 프로필을 추천·상세의 장비 판정에도 공급한다.
- FOV 원은 화면 위 평면 원 대신 구면 샘플을 카메라에 투영한다. 접안렌즈 시야는 AFOV/배율 근사. `stars-deep`의 9등급 이하 별로 원형 차트, 밝은 별 이름·DSO·상 방향·회전·경위/적도 기준·드래그 미리보기를 제공한다. **180° 도립과 상하좌우 반전은 수학적으로 같은 상**이다. 미리보기는 실제 정렬값을 수정하지 않는다.
- 스타호핑은 실제 장비 FOV의 0.8배 이내 이동, 0.5배 이내 목표 도달, 최대 7회 이동/제한 탐색. 25° 이내 4등급까지의 출발 후보를 비교해 짧은 경로를 고른다. M11에 불필요한 장거리 경로가 생기지 않도록 3등급 후보만 강제하지 않는다. **실제 FOV를 몰래 확대해 성공시키지 않으며** 실패를 설명한다. 미니 차트의 넓은 문맥 범위는 명시한다.
- 태양 고도>-6°이며 목표/정렬된 경통이 태양 15° 이내면 전면 차단. 태양은 항상 차단하며 이번 버전에는 필터 체크로 해제하는 기능을 두지 않는다. GoTo는 현재 관측자의 of-date RA/Dec 좌표 복사만 지원하며 모터 명령을 보내지 않는다.
- 실제 align1/align2/starhop/fovSetup 이벤트로 G5 미션 30개·배지 규칙 18개를 활성화한다. 정렬/스타호핑 시뮬레이션에는 관측 업적을 주지 않는다. 사용자가 실제 도입을 확인한 때 스타호핑 업적을 기록한다.
- 쌍안경도 동일한 윗변 기준 안내/정립 차트/시야 원을 쓴다. 사용자의 축 요구를 우선해 별도 후면 카메라 기준 손들기 모드는 새로 만들지 않는다. T5 태그는 실기기 정렬·드리프트 확인 뒤, G4 독립 수학 리뷰를 권한다.

## D-030 · 2026-09-08 · 방향 안내의 단계화와 대표 스타호핑 코스

- 미정렬 화면에서 숫자 대신 ‘센서 켜고 별에 맞추기’ 동작을 제공하고, 1별 완료 후 바로 이동 안내로 갈 수 있게 한다. 방향 카드에는 큰 화살표/좌우/위아래/남은 각도를 함께 표시한다. 목표에 근접해도 자동으로 차트로 바꾸지 않는다. 센서 중단은 재시작 가능한 미지원 상태와 새 세션 ID로 바꾸고 기존 정렬을 무효화한다.
- 유명 경로 6개는 원본 G5 미션과 별도의 `hopCourses.ts`에 한·영 독자 문안으로 둔다. Skyledge 관측 설명을 참고하고 외부 차트/이미지는 복사하지 않는다. 위치는 기존 HYG/DSO 팩을 사용한다. 고정 이정표의 긴 구간은 실제 FOV 기준 시야 수로 드러내고 나눠 이동하도록 안내한다. 자동 경로의 0.8/0.5 FOV 제한과 구분한다.
- 완료는 courseId/target/confirmed가 일치하고 simulated가 아닌 starhop 이벤트 + 그 이후 생성된 봤어요 관측 기록으로 계산한다. 다른 코스·모의 관측·삭제/못봄/이전 기록으로 완료하지 않는다. 이벤트는 기존 백업 progress에 포함돼 스키마 변경 없이 이동한다.
- 쌍안경 모드의 방향 안내는 경위 방식으로 고정한다. 이전 망원경 프로필에 남은 GoTo/적도의 설정 때문에 쌍안경 화살표나 차트 축이 바뀌지 않도록 분리하고 전환 회귀 테스트로 확인한다.

## D-031 · 2026-09-08 · Capacitor 앱과 Play AAB 출시 준비

- 사용자가 APK에서 AAB로 산출물을 변경했다. Android release AAB를 빌드하고 업로드 키로 로컬 서명한다. 기본 appId는 저장소 소유자 기반 `io.github.junhyoungparknobel.skylog`; 공식 등록 전 확인. 버전은 Android 0.1.0-beta.1 / iOS 0.1.0, versionCode는 mobile workflow run number.
- Capacitor 최신 안정 8.5.1 및 공식 App/Geolocation/Filesystem/Share 플러그인. PWA base=/skylog/, native base=/; native SW 미사용, 모든 데이터 포함. Android API36/min24·JDK21, iOS15+/Xcode26. 패키지 버전과 환경 요구는 npm/공식 문서 확인.
- native 상대 자세는 Android GAME_ROTATION_VECTOR / iOS xArbitraryZVertical, AR은 자북 모드. ENU 물리 quaternion을 기존 래퍼로 변환하며 +Y 경통 규약 유지. background에서 중지하고 재정렬. 권한 없음/샘플 없음은 숨기지 않고 실패 표시. 센서 실기기 정확도는 별도 검증한다.
- backup은 OS 공유 시트에서 사용자가 목적지를 정한다. 공유용 앱 캐시는 수신자가 읽기 전에 제거하지 않는다. 네이티브와 PWA DB는 별도이므로 JSON 이관. 기록 서버/계정/광고/온라인 순위 추가 없음. 날씨 좌표 전송을 privacy/스토어 초안에 명시한다.
- CI는 무서명 AAB·Android lint 및 오프라인 WebView 계측, iOS 무서명 컴파일을 수행한다. 업로드 키는 저장소 밖 Windows 사용자 전용 폴더, 암호는 DPAPI 파일에 보관하고 재사용한다. Store 계정/결제·정책 선언·실기기·심사 제출은 남은 사용자 작업이다. Xcode archive와 최종 스토어 실기기 캡처 전에는 출시 완료라고 보고하지 않는다.
- versionCode 4(108e99a)의 Android/iOS CI 및 로컬 AAB 서명/구조 검증을 완료했다. Windows `Set-Acl`의 불필요한 감사 권한 요구는 `FileSystemAclExtensions.SetAccessControl`로 접근 권한만 적용해 해결했다. 원 업로드 키는 재사용하며 후속 앱 빌드에서 다시 생성하지 않는다.

## D-032 · 2026-09-08 · 최신 AAB 재빌드·개인 APK·iPhone 설치 준비

- 사용자 직접 설치 요청에 따라 Play AAB와 함께 release APK를 제공한다. 원 업로드 키 복원/Play 등록 여부 응답 전 새 업로드 키로 교체하지 않는다. 개인 APK는 별도 `skylog-local-test-signing` 키로 서명하며, Play 서명이 다르면 JSON 백업→기존 앱 삭제→새 앱 설치→복원을 안내한다.
- `sign-aab.ps1`은 키 누락 시 중단하고 명시적인 `-CreateNewKey`에서만 최초 키를 만든다. AAB/APK는 임시 파일 서명·검증 후 최종 이름으로 옮긴다. 비밀은 저장소 밖 사용자 전용 ACL/DPAPI 보관.
- local versionCode5와 다음 CI run_number5의 충돌을 피하기 위해 CI 필수 `version_code` 입력(default6)을 검증해 Android/iOS/metadata에 함께 사용한다. 이후 Console 최대 번호보다 크게 지정한다.
- iOS 실제 root를 `SkylogViewController`로 통일해 센서 등록을 실행한다. Geolocation 필수 목적 설명을 보완하되 실제 위치 요청은 whenInUse 유지. Tailwind4/Safari 지원 하한에 맞춰 앱 최소 iOS16.4. Mac의 새 컴파일/실행과 iPhone 센서 QA는 남는다.
- iPhone 즉시 체험은 기존 HTTPS PWA 홈 화면 설치. 네이티브 TestFlight와 저장소·오프라인 포함 조건이 달라 구분한다. 웹 기능 변경은 없으며, 현재 GitHub 인증이 없어 로컬 수정은 원격에 미반영이다.

## D-033 · 2026-09-08 · 천체 상세 창 전체에서 쓸어 조작

- 사용자 실사용 불편을 반영해 작은 손잡이만 받던 제스처를 본문·제목·액션 행으로 확장한다. 반쯤 열린 창은 위로 쓸어 펼치고 아래로 쓸어 닫는다. 펼친 상태의 본문은 네이티브 터치 스크롤을 유지하고, 내용 맨 위에서 시작한 새 아래 제스처만 창을 접는다. 내용 중간에서 시작한 동작은 맨 위에 도달해도 같은 제스처로 창을 접거나 닫지 않는다.
- 펼친 창의 제목·액션 행에서도 본문을 스크롤한다. 기존 손잡이는 직접 단계 조절을 유지한다. 동작 방향은 최초 이동에서 고정하고 가로 버튼 행·입력 조작·핀치를 보존한다. 짧은 탭/키보드는 기존 버튼 동작, 세로 드래그 뒤 합성 클릭은 억제한다. 취소·둘째 손가락 추가는 단계 변경으로 확정하지 않는다.
- 마우스 이동·종료는 window에서 추적해 첫 이동이 창 밖으로 나가도 받는다. 기존 본문 마우스 관성은 새 시트 제스처 시작 시 중단한다. 공용 관성 수학을 재사용하며 천문 계산·센서·DB·데이터 팩은 변경하지 않는다.
- 직접 설치 APK와 unsigned AAB를 versionCode6으로 재빌드하고 APK는 build5 개인 키를 재사용한다. 다음 CI 기본값은7. Play 업로드 키 복원·iOS 서명/TestFlight·물리 기기 확인·GitHub 인증 후 공개 웹 배포는 별도 잔여다.

## D-034 · 2026-09-08 · 자동 방향 안내와 원형 천구

- 사용자 실사용 요구로 별길의 기본 시작을 automatic 나침반 방향으로 바꾼다. 절대 북 기준 또는 검증된 iOS 나침반 동기화만 사용하고, 상대 yaw 자체를 실제 방위로 간주하지 않는다. 정렬 없이 대략 위치를 찾을 수 있지만 좁은 망원경 시야의 정밀 별 보정은 선택으로 유지한다. 카메라 별 인식/plate solving은 구현하지 않았다.
- automatic과 relative 정밀 모드를 새 센서 세션으로 분리한다. iOS 나침반 5개 유효 샘플로 동기화하고 한동안 기울인 뒤에도 30초 이상 지난 기준은 방향 안내에 쓰지 않는다. 1.5초 센서 중단/프로바이더 변경은 기준을 무효화한다. 권한 요청은 사용자 탭에서 시작한다. 자동 추정으로 정렬 업적이나 정밀 도착 피드백을 만들지 않는다.
- 방향 안내에는 같은 SkyScene의 실제 별/선/은하수/목표를 물리 +Y 방향으로 그린다. 나침반 미지원 시 목표 주변 하늘을 미리 보여 주고 임의의 방위 화살표를 만들지 않는다. 태양 차단·정밀 보정·백업/관측 진도를 유지한다.
- 렌더러는 공통 입체 투영과 정확한 역변환을 사용한다. 축척 범위3..220에서 시선 쪽 **최대180° 반구**가 원형으로 보이며220°를 실제 시야각으로 표시하지 않는다. 원 밖 선택 금지·GPU/CPU·라벨·FOV에 같은 반경을 적용하고 원형 하늘 버튼은 천정 중심으로 이동한다.
- 밝은 별의 중심/크기·선 대비를 높이고, 기존 은하수 팩 윤곽 안에 고정된 부드러운 먼지 무늬와 색을 더한다. 별 위치/수량·데이터 팩·천문 좌표는 유지하고 야간은 적색만 사용한다. 실제 모바일 FPS·나침반 정확도는 사용자 기기에서 확인한다.

## D-035 · 2026-09-08 · 유료 배포와 두 사람의 무료 이용 설계

- 현재 오프라인 기능 중심 앱은 무료 기본 + 비소모성 Pro 한 번 구매를 제안한다. 9,900원/출시 할인4,900원은 검토 예시이며 판매 가격 확정이나 상품 등록이 아니다. 현재 체험판/웹앱 기능은 잠그거나 과금하지 않는다.
- 두 사람의 출시 후 무료 이용은 각 스토어의 공식 Pro 무료 코드로 시작할 수 있다. Android/iOS/PWA 공통 이용권은 로그인·서버 거래 검증·만료 없는 개별 무료 권한으로 연결한다. 공용 비밀번호/클라이언트 설정을 유료 권한으로 신뢰하지 않는다. TestFlight를 영구 무료 이용권으로 대체하지 않는다.
- 기존 관측·사진·학습 데이터와 유료 권한은 분리한다. 로그아웃/환불 후에도 기록 열람/내보내기를 유지한다. 현재 무료 Open-Meteo 서비스는 비상업용이므로 상업 API/서버 또는 대체 날씨안을 결정한 뒤 유료 출시한다. 공식 근거·기능/가격안·구현 순서는 `docs/MONETIZATION-PLAN.md`.


## D-036 · 2026-09-08 · 하늘 가독성과 반투명 기본 화면

- 사용자 실사용 요구를 따라 별자리 연결선과 경계는 낮/밤 테마에서 흰색으로 통일한다. D-034의 야간 적색 규칙은 이 두 레이어에 한해 예외다. 다른 야간 렌더는 적색을 유지한다. 경계 켜짐/꺼짐 등 저장된 레이어 선택은 유지한다.
- 별은 밝기에 따른 차이를 유지하며 중심과 최소 크기를 키운다. 은하수의 밀도·색·alpha 중복 감쇠를 제거하고 부드러운 밝기 곡선을 쓴다. 실제 낮에는 대기 효과로 숨기고, 낮의 지도 열람은 대기 효과 끄기로 가능하게 안내한다. 별 좌표/개수·데이터 팩은 바꾸지 않는다.
- 시간 바는 현재 시각에서 접힌 작은 버튼이 기본이며 수동 시간은 날짜·현재로 복귀를 표시한다. 하단 탭은 52px와 46% 반투명 표면(야간58%)을 사용하고 투명도 감소 환경에는 읽기 쉬운 대체 표면을 제공한다. 원형 하늘·별길·실제 하늘·선택 정렬은 하늘 설정에 모은다. 글자 확대 및 센서 안내 높이에 따라 목표 HUD 위치를 계산한다.

## D-037 · 2026-09-08 · 위치·방향 센서 기본 자동 사용과 선택 유지

- 방향 센서는 하늘 진입·복귀 시 기본 자동 시작한다. 명시적 끄기는 저장하고, 고정 시점/시간 링크나 원형 지도 보기에서는 자동 회전을 중지한다. iOS 동작 권한은 사용자 제스처에서만 요청하며 권한 거부/센서 미지원은 켜짐으로 가장하지 않는다. 상대 yaw는 북 기준을 얻기 전 방향에 사용하지 않는다.
- 현재 위치 자동 사용은 새 설정의 기본값 true다. 앱 시작·복귀에 한 번 요청하며 30초 안 재요청을 제한하고 백그라운드에서 중단한다. 최초 OS 권한 허용이 필요하며 거부 이력을 존중한다. 저장 관측지를 선택하면 자동 위치를 끄고 선택을 보존한다. 기존 사용자 기본 관측지도 보호한다. 자동 갱신을 끈 경우 마지막 GPS 위치를 유지한다.
- 초기 저장소 복원·권한 대기·화면 이동과 늦은 센서/GPS 응답을 세대 번호로 구분해 사용자의 최근 선택을 덮어쓰지 않는다. 별 보정 재사용은 같은 센서 기준과 위경도 각각 0.01° 이내에서만 허용한다. 좌표 없는 이전 보정은 재사용하지 않고 세션 중 먼 위치로 이동하면 보정을 해제한다.

## D-038 · 2026-09-08 · 하늘 기본값·단일 지면 설정·천체 표시 크기

- 사용자 실사용 피드백에 따라 새 기본값은 별자리 경계 false, 은하수 alpha 0.33, 지면 불투명도 1, 별 채도 1이다. 하늘 설정의 기본값 복원으로 이 구성을 다시 적용한다. 기존 사용자 수치는 보존하되 이전 기본 은하수 0.6/별 채도 0.8은 새 기본값으로 옮긴다. 경계 선택은 저장된 값을 유지한다.
- D-027의 겹치는 ground/showBelowHorizon/groundOpaque는 `groundOpacity` 하나로 대체한다. 내부 불투명도 1에서는 지평선 아래 표시와 선택을 모두 막고, 1 미만에서는 같은 설정으로 표시·선택한다. 사용자 UI는 **지면 투명도**이며 0%=불투명, 100%=투명으로 표시한다. 명시적인 이전 지면 숨김은 투명으로, 이전 기본 반투명 지면은 불투명으로 옮긴다. 실제 가시성·추천·관측 미션의 고도 판정은 바꾸지 않는다. layers persist 버전은 2이며 Dexie DB v2는 유지한다.
- 별 표시 크기를 1.2배 키우고 기존 은하수 윤곽 위의 고정된 밝은 띠·먼지 결·색 대비를 조정한다. 별 좌표·개수·기존 천문 데이터 팩은 유지한다. 은하수의 색은 시각 표현이며 사진 또는 새 관측 데이터가 아니다. 야간 은하수는 적색, 별자리 연결선/경계는 D-036의 흰색 예외를 유지한다.
- 태양·달의 화면상 최소 지름은 24 CSS px다. 실제 각지름을 계산하는 천문 값은 유지하며 표시 크기만 보정한다. FOV 변경 뒤 표시·라벨·선택 반경을 함께 갱신한다. 태양 관측 안전 차단은 유지한다.

- 달은 별·선과 같은 렌더 큐에서 그리되 opacity 1로 뒤쪽 천체를 완전히 가린다. 확대된 달의 어두운 면 위로 배경 별이 다시 그려지던 순서 문제를 수정했다. 실제 GPU 픽셀 차분으로 달 뒤 별의 밝기 기여가 0이 되는지 확인한다.

## D-039 · 2026-09-08 · 두 장비 시야원과 사용자 입력 가능한 시작 예시

- 사용자는 솔로몬 HQ 8×42 ED와 SVBONY SV48P 102mm를 알려주었고, 실제 시야와 접안 사양은 사용자가 입력하도록 하되 우선 보편적인 예시로 시작하기로 했다. SV48P 102mm의 구경 102mm·초점거리 663mm는 [제조사 제품 정보](https://www.svbony.com/products/sv48p-telescope-refractor-102mm)로 확인했다. 구형 SV48P 90mm/500mm와 별도 모델이다.
- 쌍안경 7.50°와 망원경 접안렌즈 25mm/AFOV 52°는 시작용 예시이며 솔로몬 제품의 실제 시야나 사용자가 보유한 접안렌즈 사양으로 단정하지 않는다. 망원경 실시야는 기존 근사식 AFOV/(망원경 초점거리/접안 초점거리)로 약 1.96°다. 구경만으로 FOV를 유도하지 않는다.
- 기본 시야원은 쌍안경과 망원경 두 개이며 파인더를 세 번째 기본 원으로 그리지 않는다. 전체 `fovRings` 설정을 유지하고 개별 선택·장비명·각도·예시 표시·사양 편집을 제공한다. 폼 입력의 계산은 즉시 갱신되고 저장하면 하늘 시야원에 반영된다.
- telescope persist 버전 1에서 이전 기본 프로필의 모든 필드가 같고 저장 장비 ID가 없을 때만 새 예시로 갱신한다. 커스텀/저장 장비와 기존 시야원 켜짐·꺼짐은 유지한다. 각 장비 예시 표시는 관련 사양을 직접 입력할 때 해제한다.

## D-040 · 2026-09-08 · 확장 업적 팩 v2와 기존 진도 호환

- 기존 18개 배지에 30개를 더해 총 48개 업적을 제공한다. 단계·진행도·다음 업적 안내를 더하되 실제 관측/학습 기록과 검증된 기능 이벤트에서 조건을 계산한다. 자동 나침반 추정이나 모의 정렬로 정밀 정렬 업적을 만들지 않는다.
- 확장 업적은 `public/data/learn/v2/badges.json`과 manifest(version 2, legacyCount 18)에 게시한다. 기존 `learn/v1` 배지 ID와 규칙을 보존하며 코스·미션·퀴즈는 v1을 계속 사용한다. 백업/복원의 기존 기록·배지·퀴즈 진도를 새 팩 때문에 초기화하지 않는다.
- 이는 T7 전체 완료가 아니다. skyPick 36문항, 하루 복습 누적 상한과 영구 획득 이력 등 남은 항목은 STATUS의 후속 작업을 따른다.

## D-041 · 2026-09-08 · 센서 프레임 보간과 같은 설정의 중복 저장 제거

- 센서 이벤트에서 카메라 quaternion을 즉시 교체하던 경로를 렌더 시각 보간으로 바꾼다. 실제 샘플 간격을 추정해 최대 50ms 안에서 최단 경로로 잇고, 입력이 끊겨도 미래 방향을 예측하지 않는다. 동일 목표는 정착을 미루지 않으며 첫 입력·250ms 이상 공백·명시 중단은 이전 보간 상태를 초기화한다.
- 실제 카메라/선택은 렌더 프레임마다 갱신하고 React용 상태 알림은 10Hz와 최종 정착 시점으로 제한한다. keepLevel은 중간 보간 중에도 롤 0을 유지한다. 기존 필터 상수·북 기준 판별·1.5초 센서 중단 무효화·수동 드래그 5초 일시 정지는 유지한다.
- 합성 테스트의 불규칙 20–45Hz 입력/11–22ms 렌더 간격에서 프레임 각속도 RMS 오차가 즉시 교체 방식의 절반 미만, 평균 추가 지연 25ms 미만이다. 마지막 샘플 도착 후 최대 50ms에 정착하고 기존 필터를 포함한 90° 스텝은 150ms 이내 도달한다. 실제 Android/iPhone FPS나 물리 센서 성능을 측정한 수치가 아니다.
- Dexie settings 어댑터는 네임스페이스별로 성공한 마지막 직렬값과 같으면 반복 쓰기를 생략한다. 읽기/삭제에서 캐시를 무효화하고 작업을 순서대로 처리하며 실패한 쓰기는 다음 시도에서 다시 저장한다. 데이터 저장 형식·DB 스키마·실제 persist 값은 유지한다.

## D-042 · 2026-09-08 · 날씨 요약과 현재 달 기준 천문 달력

- 관측 조건은 ‘날씨’로 부른다. 기온·구름·비 확률·바람/시간별 예보를 먼저, 밤하늘의 달빛 적은 시간·일몰·달은 다음에 둔다. 상세 정보는 펼치며 기존 원본과 관측 판정을 유지한다. 구름 적은 구간의 경계를 사용자가 고른 시간 안으로 제한한다.
- 목록을 기본으로 월간/연간 보기를 추가한다. 월 이름은 현재 현지 달력 날짜와 +1개월로 계산하며 선택한 월에 따라 중복되지 않는다. 분 경계·앱 복귀·시계 변경을 감지하고 월말 자정에 현재 월로 이동한다. 연간 계산은 한 달씩 나누고 키보드 날짜 탐색을 지원한다.
- 한 해 일정은 기존 천문 계산으로 ICS를 만든다. UTC 시각·안정 UID·UTF8 줄 접기를 적용하며 기존 기기 공유/다운로드를 쓴다. 자동 동기화·새 일정 알림·개인 메모 저장 기능은 포함하지 않는다. 신규 패키지는 없다.

## D-043 · 2026-09-08 · 장비 테마부터 고르는 코스와 스타호핑 거리 표현

- 맨눈/쌍안경/망원경→세부 코스→미션 구조다. 망원경→스타호핑 입문에서 기존6개 경로를 고른다. 기존 ID/진도/기록·업적·백업·딥링크는 유지하고 theme/group 해시만 추가한다.
- 시야를 물건처럼 ‘0.2개’로 세지 않는다. 한 시야보다 짧으면 보이는 원 너비의 비율, 길면 너비의 배수와 겹쳐 이동할 횟수로 설명한다. 실제 각거리·사용 장비의 시야각 계산은 유지한다. 한국어/영어가 같은 의미를 전달한다.

## D-044 · 2026-09-08 · 낮은 장식 풍경과 아래 시선의 점진적 투명화

- 사용자 요청에 따라 낮은 잔디/꽃의 독자 생성 이미지를 기본 제공한다. 실제 관측지 지형을 나타내지 않는다. 생성 프롬프트와 출처는 docs/TONIGHT-REFRESH.md, 에셋은 public/landscapes/meadow-v1.webp다. PWA/native 오프라인에 함께 넣고 야간은 적색으로 처리한다.
- D-038의 수동 groundOpacity에 시선 중심 고도0°→−28° smoothstep 감쇠를 곱한다. 풍경 하단도 부드럽게 지면과 섞는다. 렌더·라벨·별 선택·지평선 아래 안내가 같은 유효 불투명도를 사용한다. landscape=false면 기존 수동 지면 동작이다.
- 사용자 저장 투명도는 보존하고 landscape:boolean만 추가한다. DB·천체/학습 팩 포맷은 바꾸지 않는다. 천체 좌표·실제 관측 가능 판정·태양 안전 차단과 분리한다. 기존 천구 투영/센서/장비는 유지한다.

## D-045 · 2026-09-08 · 풍경 질감과 단일 지면 합성

- D-044의 별도 풍경 메쉬를 제거하고 아래 반구에 v2 식물 텍스처를 직접 혼합한다. 두 겹 alpha로 50%가75%가 되던 현상을 없앤다. 경계/수평 이음 교차 혼합, 기기 지원 최대8x 이방성 필터링, 야간 적색을 적용한다.
- 원본은 실제1774×887이며 요청한3840×1920이 아니다. 빈 공간 없이 식물 질감에 픽셀을 사용하고6회 반복한다. WebP805,980bytes. v1은 Git 이력에 보존하며 앱에 중복 내장하지 않는다. 프롬프트와 실제 크기/제약은 docs/LANDSCAPE-REFINEMENT.md.
- 아래 시선0→−28° fade, 설정/선택/센서/천체·학습 팩/DB는 유지한다. 픽셀 합성/야간과 기존 회귀로 검증했다.

## D-046 · 2026-09-08 · 무료 핵심 경험과 단계별 꾸미기·소통

- “나중” 요청은 후속 설계로 구체화한다. 로컬 마당/아바타 보상→계정·천체별 사진과 운영 도구→댓글→선택형 유료화. 실제 서버/상품을 만들거나 판매하지 않았다.
- D-035의 기존 장비/심화학습 Pro 제안을 대체하는 추천안: 현재 모든 관측/코스/퀴즈/기록/백업은 무료, 업적 보상도 무료. 첫 유료화는 선택형 외형 팩, 클라우드 원본/동기화 구독은 실제 서비스 이후. 가격은 검증 전 후보이며 사용자의 판매 결정은 아직 없다.
- 사진 공개는 별도 확인, 공개 사본 GPS 제거, 비공개 원본 접근 제어. 신고·차단·검토/이의 신청은 출시 전 준비하고 유료 기능으로 제한하지 않는다. 보상/순위와 결제를 분리한다.
- 기존 두 사람의 무료 권한 제안·거래 검증/복원·기록 보존은 유지한다. 공식 출처와 판단 구분은 plan/research/2026-09-08-community-monetization.md, 제품 상세는 docs/COMMUNITY-AND-CUSTOMIZATION.md.
- 날씨 무료API는 비상업용이므로 실제 유료 판매 전 상업용 이용/키 보호/비용을 해결한다.

## D-047 · 2026-09-08 · 업데이트 APK와 기존 개인 인증서 보존

- 사용자가 APK를 다시 명시적으로 요청했으므로 AAB와 함께 release APK를 CI에서 만든다. 앱ID 유지, build11. 아직 설치 서명 전인 산출물은 unsigned라고 구분한다.
- 기존 build9 APK의 공개 SHA256을 대조한 뒤 공개 인증서를 추출한다. 업데이트 서명은 동일 인증서 SHA256과 맞는지 검증한 뒤 최종 파일을 만든다. 누락된 개인 키를 다른 키로 대체하지 않는다.
- 이 PC에는 기존 Play 업로드 키는 있지만 개인 테스트 키가 없다. sign-test-apk.ps1은 누락 시 중단하며 새 키 생성은 명시적 최초 설치 -CreateNewKey에 한정한다. 기존 PC에서 실행할 서명 패키지를 제공할 수 있다. DPAPI 비밀번호 파일은 일반적으로 원래 Windows 사용자/PC에서만 해독되므로 폴더 복사만으로 복원된다고 약속하지 않는다.

## D-048 · 2026-09-08 · 꾸미기와 소통까지 전부 무료

사용자의 최신 요청으로 D-035/D-046의 유료화 후보를 현재 범위에서 대체한다. 꾸미기·업적 보상·사진·댓글·신고·개인 백업에 결제나 구독을 도입하지 않는다. 서버 용량/발송 한도는 존재하지만 결제로 우회하는 UI를 만들지 않는다. 이전 시장 조사는 참고 이력으로만 보존한다.

## D-049 · 2026-09-08 · 다섯 자리 마당과 영구 업적 보상

하단 다섯 탭을 유지하고 독립 profile 경로에 마당/아바타를 둔다. 네 기본 장식과 여덟 업적 보상. 보유/잠김 목록 분리, 그림과 버튼에 대응하는 자리 번호, 키보드 선택을 제공한다. progress의 고유 키와 트랜잭션으로 중복 지급을 방지한다. 장식은 한번 얻으면 영구 보유하며 기존 기록·퀴즈 판정/DB/팩 형식은 바꾸지 않는다. 색·모자·배치는 기존 백업으로 보존하고 입력을 정규화한다.

## D-050 · 2026-09-08 · Supabase 사진 검토와 접근 제어

사용자 요청에 따라 무료 Supabase 조직/서울 프로젝트를 생성했다. RLS·제한된 RPC·실제 운영 역할, private 이미지 버킷을 적용했다. 서버 JPEG 재인코딩으로 EXIF를 제거한다. 신규/수정 사진과 댓글은 검토 후 공개한다. 신고·상호 차단·사유·이의 신청·운영 조치를 저장하며 작성자가 나만 보기로 바꾸면 운영 UI에서도 임의 공개할 수 없다. 사진 삭제로 일일 업로드 횟수가 초기화되지 않는다. 공개 서명 URL은60초이며 이미 받은 사본을 회수한다고 약속하지 않는다.

## D-051 · 2026-09-08 · 개인 백업과 단계적 가입 개통

백업은 계정별 비공개 스냅샷(최대5개·각20MB), 명시적 업로드/검증 후 최신 병합 복원이다. 자동 동기화로 표시하지 않는다. 토큰은 로컬 DB 내보내기에서 제외한다. 계정 변경 시 화면과 복원 미리보기를 새로 만들며 계정 삭제는 온라인 파일부터 제거하고 기기 기록을 보존한다. 기본 Supabase 메일은 조직 구성원 주소만 지원하며 무료 기본 발송에서 템플릿 변경이 거부됨을 실제 확인했다. SMTP·운영자 지정·외부 주소 검증 전 일반 가입을 개통하지 않는다. 웹 PKCE 메일 링크와 추후 앱 내 OTP 입력을 지원한다.

배포 확인: beta.7/build12 웹·Android·iOS CI와 실제 서버 권한 검사를 통과했다. 마이그레이션 다섯 개의 적용 이력은 로컬/원격 일치를 확인했다. 공개 빌드의 SIGNUPS_READY/EMAIL_CODE는 false 기본값을 유지하며 사용자가 만든 Supabase 프로젝트만으로 외부 메일 발송까지 완료됐다고 취급하지 않는다. D-047의 동일 APK 인증서 원칙에 따라 서명된 Play AAB와 개인 키 없는 APK 서명 준비 패키지를 구분해 제공한다.

## D-052 · 2026-09-09 · 아바타 미리보기·영구 보상과 세 코디

집의 최신 main f5cf178을 기준으로 기존 마당/아바타 필드를 보존하고 머리·표정·의상·소품을 추가한다. 39개 선택 중 기본33은 무료, 새6개는 기존 관측/학습 업적에 연결하며 한번 지급한 보상은 영구 보유한다. 기존12개 마당 장식·천문/학습 팩·DB스키마·잔디밭을 바꾸지 않는다.

편집은 미리보기와 명시 적용/취소이며 추천은 보유 선택만 이용한다. 코디3칸은 이름과 AvatarLook만 보관하고 실제 아바타에는 적용 버튼으로 반영한다. progress의 personal.looks와 avatar.reward:<category>:<value>를 기존 백업에 포함하고 입력을 정규화한다. 마당/이름/아바타/다른 코디를 최신 트랜잭션 안에서 보존하며 저장 후 최신 화면 읽기까지 다음 편집을 막는다. 읽기 실패는 재로딩 전 이전 데이터로 재편집할 수 없게 처리한다. 이는 무료 로컬 성취 저장이며 유료 거래 검증이 아니다.

기존 SVG를 확장하고 고유 ID의 야간 필터·큰 미리보기·부위별 그림 선택과 잠금 조건/진행도의 접근 가능한 설명을 제공한다. 웹/개인 APK로 배포하고 실제 기기 확인은 사용자에게 전달한다. 개인 APK는 기존 build9 인증서와 같아야 하며 새 키로 대체하지 않는다.

## D-053 · 2026-09-09 · 무료 범위를 보존하는 테마 판매 재검토

사용자가 유료화할 부분을 다시 생각해 달라고 요청했으므로 D-048의 현 기능 무료를 유지하며 향후 별도 제작 외형 팩1종의 비소모성 1회 구매를 제안한다. 이번 아바타 확장·기존 보상·현재 수동 계정 백업도 무료다. 3,900원은 검토용 가설이며 상품/가격/거래/유료 권한을 등록하거나 실행하지 않았다. 구독은 추가 원본/자동 동기화 서비스와 지속 비용을 확인한 후에만 검토한다.

기존 Supabase 계정을 활용해 서버 검증 거래와 계정별 이용권을 별도로 구현하고 본인/GF 무료 권한도 실제 UUID에 감사 이력을 남기도록 계획한다. 공식 스토어 무료 거래와 별도 무상 권한을 구분하고 서버 grant만으로 IAP 규칙이 면제된다고 단정하지 않는다. 판매 전 날씨 API 상업 이용 계약·키 보호·운영비도 준비한다. 공식 자료와 판단 경계는 plan/research/2026-09-09-monetization.md, 실행안은 docs/MONETIZATION-PLAN.md다.

D-052 배포 확인: 최종 소스 d478b9b, beta.8/build13 서명 APK와 PWA 공개 검증 완료. 단위528·브라우저66·WebKit8 통과. Android versionName은 package.json을 단일 기준으로 읽고 CI에서 검사한다. 기존 개인 APK와 같은 인증서로 업데이트하며 Play용 AAB의 최종 서명은 이 PC에 없는 기존 업로드키가 필요하다.

## D-054 · 2026-09-09 · 향후 심화 퀴즈·망원경 학습 이용권

사용자의 구체적 요청에 따라 D-053의 외형 팩 우선 제안을 향후 난이도2·3 퀴즈와 망원경 학습 코스로 대체한다. 현재 전 기능 무료와 진도/보상/기록을 유지하며 이번 버전에는 잠금·결제·가격·이용권 발급을 구현하지 않는다. 두 영역은 추가 이중 결제를 피하는 비소모성 이용권1종 후보이며 가격/시행일은 후속이다. 맨눈·쌍안경 무료 미션이 어려운 확인문항 때문에 막히지 않도록 신규 쉬운 문항과 missionKey/진도 이관을 먼저 설계한다. 장비 설정/FOV/일반 목표 찾기는 무료 관측 도구다. 본인/GF 무료 권한은 실제 계정별 서버 검증·스토어 정책/복원 절차와 함께 준비한다. docs/MONETIZATION-PLAN.md와 범위 매핑 참조.

## D-055 · 2026-09-09 · 권리 확인된 대표 관측 사진과 오프라인 표시

ESA/Hubble·ESO의 개별 이미지 페이지와 CC BY 4.0 정책으로 상업 재배포/변형을 확인한 실제 관측 사진20개를 앱에 넣는다. 사진은 카탈로그 CC BY-SA/코드MIT와 별도 라이선스이며 기관 로고·권리 예외·삽화를 관측 사진으로 넣지 않는다. 모든 미리보기/상세에 전체 원문 크레딧·활성 저작자/출처/라이선스 링크·축소/WebP·야간 적색 변환을 표시한다. 개별 원본/권리 확인/해시/가공은 manifest와 docs/OBJECT-PHOTOS.md, XMP에 보존한다. 촬영 시점·합성 파장·세부/넓은 범위를 구분하고 실제 접안 시야/현재 실시간 모습으로 안내하지 않는다. 야간 원래 색 보기는 명시 선택이며 대상·테마 전환으로 초기화한다. 로컬 이미지 실패는 정보/검색을 막지 않으며 PWA 캐시와 native assets를 검증한다.

## D-056 · 2026-09-09 · 댓글 초안·페이지·계정 경계 보완

댓글과 사진 신고/이의 사유 상태를 분리하고 전송 대기 중 다음 초안이 지워지지 않게 편집을 보호한다. 사진 ID·계정 ID로 댓글 UI를 분리해 늦은 읽기/쓰기 응답이 다른 화면을 바꾸지 않게 한다. 최신50개와 (created_at,id) 커서로 이전 댓글을 조회하고 작성 성공과 후속 조회 실패를 분리해 중복 재전송을 피한다. 승인/상호 차단/신고/삭제/속도 제한은 기존 RPC/RLS 정책을 유지한다. 로컬 DB/모킹 시험과 실제 공개 GET 결과를 구분하며 일반 가입/메일/운영자 개통 전 플래그를 켜지 않는다. docs/COMMENTS-REVIEW.md 참조.

D-055/D-056 배포 확인: 소스 `9f0047d084048d8f5831467771cead6835563a69`, beta.9/build14 공개APK와 PWA, Pages34303341473/모바일34303357539 전체 성공. 단위547개 고유 검사·DB46·Chromium75 통과, 사진40개 공개/네이티브 해시와 iOS public191개 일치. WebKit 온라인/야간 등7단계는 통과했으나 Windows 오프라인 내부 오류로 실제 iPhone 오프라인은 미확인이다. 출처/검증 문서와 해당 경계를 기록하며 앱/스토어 전체 수용으로 확장하지 않는다.

## D-057 · 2026-09-09 · 간결한 사진 미리보기와 공개 관측 자료 확대

사용자 요청에 따라 선택 카드·검색에는 사진과 천체 정보를 우선 표시하고 긴 사진 출처는 자세히의 큰 사진 아래와 앱 정보에서 제공한다. D-055의 모든 미리보기 인접 전체 크레딧 표시를 변경하므로, 숨김 금지 조건이 있는 기존 ESA/Hubble·ESO 자료는 그대로 숨기지 않고 적합한 사진으로 교체한다. IPAC 2MASS의 명시적 public-domain 갤러리를 메시에 전체 사진의 기반으로 하며 추가 자료도 개별 원문·권리 근거·제작자·해시를 확인한다. 적외선/복수 파장과 일부/전체 영역을 구분한다. 코드MIT·카탈로그CC BY-SA를 사진에 일괄 적용하지 않는다. 사진은 로컬 번들/PWA 프리캐시로 제공하며 야간 적색·명시 원색과 오류 대체를 유지한다.

확정 묶음은 메시에 110개를 포함한 DSO 153개·태양계 9개·별 2개, 총 164개 천체/328개 WebP/11,787,848 bytes다. 원본은 162개 고유 URL이며 두 쌍의 공유 모자이크를 캡션에 명시한다. 사진 manifest는 원본 픽셀 크기 `sourceDimensions`와 검토한 `crop`을 추가해 **version 2**로 올리되 배포 경로는 호환성을 위해 **`object-photos/v1/`**을 유지한다. 기존 천문 데이터 팩 버전 변경과는 별개다. 2MASS의 사진 밖 제목·로고 영역과 베텔게우스의 전체 관측 패널 선택만 허용하며, 과학적 삽입 사진·연결선과 어두운 관측 외곽을 보존한다. M64는 관측 영역 위에 겹친 원본 세로선 두 개를 남긴다. 전체 입력/출력 SHA·크롭 범위·XMP와 미사용 파일 0을 검증했고, 최종 연락 시트 01~05는 담당 에이전트, 06~09와 경계 예외는 루트가 직접 확인해 승인했다. 상세 근거는 [사진 문서](../docs/OBJECT-PHOTOS.md)와 공개 manifest에 보존한다.

## D-058 · 2026-09-09 · PKCE를 유지하는 메일 로그인 복귀와 복구

메일 수신만으로 로그인이 완료됐다고 표시하지 않는다. 네이티브 앱은 정확히 허용 등록한 `io.github.junhyoungparknobel.skylog://login`으로 메일 확인 후 바로 복귀한다. 웹 Site URL과 구 앱의 기본 HTTPS 경로는 유지한다. 같은 브라우저에서는 PKCE 교환을 마치고, 외부 브라우저에는 설치한 Android 앱으로 일회용 코드를 돌려주는 버튼과 iPhone/PWA의 복구 링크 복사를 제공한다. 원래 앱의 verifier로만 PKCE 세션을 얻는다. 원본 확인 링크 붙여넣기는 허용된 Supabase URL을 검사하고 격리 클라이언트에서 인증한 계정이 실제 요청 이메일과 같은지 확인한 뒤 세션을 저장한다. URL의 인증값은 즉시 정리하고 백업·로그에 넣지 않는다. 늦은 응답/중복 전송/만료와 앱 cold/warm 시작을 검사한다. 서버 기본 SMTP에서는 숫자 코드 템플릿을 적용할 수 없어 EMAIL_CODE=false를 유지한다. 이메일 확인·RLS를 완화하거나 새 로그인 제공자/결제 서비스를 임의로 연결하지 않는다. 실제 메일 발송·실기기 왕복은 자동 모킹 검사와 구분한다.

이미 다른 계정으로 로그인한 외부 브라우저에서도 새 링크의 복구 안내를 먼저 표시한다. 현재 계정으로 돌아가기는 콜백만 닫으며 세션·기존 북마크를 지우지 않는다. 느린 인증 응답 중에는 계정 변경 작업을 막고 오류 종류에 맞춰 다시 받기 또는 복구를 안내한다.

D-057/D-058 최종 배포: 앱 소스 `62df2eab0f5da518e445ca9a7d642afdfc6c595a`, beta.10/build15 공개 APK와 PWA, Pages34320972447/모바일34320977187 성공. 단위580·DB46·Chromium83·Windows WebKit14·Android API36 계측2개 통과. 공개 APK 재다운로드와 사진328개 해시, 실제 iOS CI 무서명 앱의 public479개/사진328개 일치를 확인했다. 출처·로그인 복구 문서와 최종 소스를 함께 보관한다. Play 업로드 키 서명·Apple 서명/TestFlight·일반 회원 SMTP·실제 폰 메일 왕복/센서/iPhone 오프라인은 후속 확인이며 현재 모든 기능은 무료다.

## D-059 · 2026-09-09 · 오늘 밤에도 공용 천체 사진 적용

추천·관측 계획·오늘의 볼거리가 사용하는 공용 행에 기존 검색의 PhotoThumbnail을 연결한다. 천체 ID에 사진이 있을 때만 실제 사진을 보여 주며, 사진 없음/읽기 실패는 종류 기호로 대체한다. 사진 유무 때문에 추천 순서나 관측 점수를 바꾸지 않는다. 관측/계획 별 버튼은 계속 구분하고, 전체 출처는 상세 화면과 앱 정보에 둔다. 사진 목록·권리·manifest와 추천 엔진은 변경하지 않는다. beta.11/build16·앱 소스 e672ca13e98da1eab747f5c7a2762afe6625d919로 APK/PWA를 배포했고 타입/lint·단위580·관련 Chromium9·Chromium/WebKit 화면·공개 사이트와 APK 해시를 검증했다. 실제 폰 검증과 새 iOS 컴파일은 자동 UI 확인과 구분한다.

## D-060 · 2026-09-10 · Skyard 브랜드와 현행 출시 문서

사용자가 확정한 표시 이름은 ‘스카이야드 Skyard - 천체 관측 가이드’이며 짧은 한글 이름 스카이야드/영어 Skyard를 쓴다. 기존 io.github.junhyoungparknobel.skylog·로그인 URI·DB/백업 식별자는 보존한다. 공개 지원 이메일 tony030214@gmail.com과 운영자 박준형은 사용자 제공값이다. 외부 삭제 안내에서 웹 계정과 로그인 불필요한 이메일 요청을 제공하고, 공개 페이지 생성과 실제 요청 처리 성공을 구분한다. 개인정보 표와 Apple manifest는 선택 계정/UGC/백업을 포함하며 무수집으로 신고하지 않는다. 실제 SMTP·운영자·심사 접근/서버 보관·연령/국가·실폰/TestFlight는 미완료로 명시한다. 현재 무료 상태와 미래 난이도2·3/망원경 코스 이용권 계획을 구분한다.

## D-061 · 2026-09-10 · 최초 Play 업로드 전용키를 현재 PC에 준비

사용자가 이 패키지를 Play에 한 번도 업로드하지 않았고 집 PC를 사용할 수 없으며 지금 PC에서 계속 서명하라고 확인했다. 따라서 이전의 집 PC 키 대기 방침을 변경하여 현재 PC에 별도 RSA4096 업로드 전용 PKCS12를 생성했다. 임의 암호는 현재 사용자 DPAPI·전용 ACL로 보호하고 키/암호를 저장소·릴리스·채팅에 포함하지 않는다. 공개 SHA256 f5ad3a778d18b33973938b40f5b93f604c0cb099095a9875b09aa4f0c57a98bd. 기존 개인 APK 인증서는 그대로 유지한다. Play 최종 앱 서명키와 업로드키는 별개이며 실제 첫 Console 등록 전에는 등록 완료라 표시하지 않는다. 새 키를 집 PC 키 복구라고 부르지 않는다. 별도 암호의 휴대용 복구 백업은 사용자 로컬 입력으로 마련해야 하고 DPAPI 폴더 복사만을 타 PC 복구로 보장하지 않는다.

D-060/D-061 최종 검증: 소스 830c974bebeab0e873925ba58b0bd8a01798de6e·beta.12/build17. 현재 PC 업로드키로 서명한 AAB SHA256 888b5acf93b879f33f3f6ed9e8f57771a3be1dcc4a9a8d9b1fb5e98eb52d0d6d와 payload997개 전체 서명·원본 동일성을 검증했다. 기존 개인 APK 인증서·public480/photo328 일치, Pages34423116491 및 공개 APK 재다운로드 확인. 현재 PC의 서명 문제는 해결했고 실제 첫 Play 등록·12명14일 비공개 테스트·일반 SMTP/운영·Apple 서명/TestFlight·실폰·휴대용 키 백업은 후속이다.

## D-062 · 2026-09-10 · 광고 미도입과 계정 유지 데이터 삭제 안내

사용자는 광고 도입 제안을 철회하고 광고를 넣지 않기로 확정했다. 기존 광고 SDK 없음·현재 전 기능 무료를 유지하며, 9,900원은 제안 단계로 실제 상품을 등록하지 않는다. Play의 선택적 데이터 삭제 URL에는 `delete-account.html#delete-data`를 사용하도록 기존 공개 페이지에 한영 안내를 보완한다. 사진/백업 개별 삭제의 기존 UI·소유권 확인·파일과 행 제거를 설명하고 이메일 요청은 공개 지원 주소를 사용한다. 실제 삭제나 이메일 발송은 실행하지 않으며 운영자가 소유권 확인 후 처리한다. 계정/선택하지 않은 자료/기기 파일은 남으며 사진 링크 유효기간은 최대 60초다. 운영/검토/접속 로그의 정확한 보관 기간을 코드로 확정할 수 없어 임의 기간을 약속하지 않는다. 개별 댓글의 상태 변경을 물리 삭제로 안내하지 않는다. 정적 안내 페이지 배포와 build17 네이티브 산출물은 구분한다.


## D-063 · 2026-09-10 · 실제 UI와 부드러운 글꼴로 스토어 등록 자료 제작

현재 beta.12/build17의 기능을 기준으로 스카이야드 한국어 소개를 정리한다. 간단한 설명은 별자리 찾기부터 망원경 스타호핑까지 관측의 폭을 보여 주고, 상세 설명에는 숙련된 망원경 관측자를 위한 기준별·이동 각도·장비 시야·6개 코스를 넣는다. 자동 카메라 식별·모터 제어·전문 장비 정밀도를 약속하지 않는다. 카테고리는 교육, 태그는 과학 교육을 우선하고 날씨를 보조로 권한다. 실제 결제 기능이 없는 현재 버전에 유료 문구를 넣지 않으며 상품/구매/복원과 대상 기능이 적용될 때만 “일부 퀴즈와 망원경 코스는 별도 구매가 필요해요.”를 짧게 추가한다. 가격과 이용 기간은 확정하지 않는다.

휴대전화7장과 태블릿8장은 실제 native 정적 번들을 격리 브라우저에서 실행해 촬영한다. 실제 휴대전화/태블릿 촬영이나 센서 실기기 검증으로 표현하지 않는다. 기록 화면은 명시된 샘플만 쓰고 기본 아바타 선택 외에 진도나 보상을 채우지 않는다. 휴대전화에는 상단19% 이내 소개 문구를 배치하고 태블릿에는 추가 홍보 문구를 넣지 않는다. 스타호핑 차트10.8°와 이동량의 기준인 장비 시야6.0°를 구분해 적는다. 제목은 OFL 고운바탕, 부제는 OFL 고운돋움, 사진 출처와 원본 manifest는 보존한다. 기존 아이콘은 도안을 유지하며 Play의 모서리 마스크에 맞춰 제출용 배경만 정사각형으로 내보낸다. 업로드 PNG·TXT·설명서를 제공하며 앱 빌드나 Console 제출을 수행한 것으로 표시하지 않는다.


D-063 문구 수정: 사용자는 큰 제목의 감성적인 표현보다 담백한 기능 소개를 선호한다. 대표 문구는 “하늘 지도와 천체 관측 가이드”, 휴대전화7장은 하늘 지도·오늘 밤 추천·천체 사진·스타호핑·기록·학습·꾸미기를 직접 설명하는 제목으로 바꾼다. 퀴즈 부제도 기능 중심으로 정리하며 기존 글꼴·실제 UI·사진 출처·유료 안내 조건은 유지한다. 수정본은 v2 ZIP으로 구분한다.

## D-064 · 2026-09-10 · Plus 1회 구매 준비와 역사 천체물리

사용자는 9,900원 1회 구매를 확정하고 유료 콘텐츠 구현을 지시했다. 이전의 계획 전용·가격 미정 방침을 대체한다. 광고와 구독 없이 난이도 2·3 퀴즈, 역사 천체물리 10개 이야기·30문제·90힌트, 망원경 10미션·6개 스타호핑 코스를 묶는다. 무료 코스에 지정된 확인 문제 20개는 해당 무료 미션 안에서 계속 풀 수 있다. 기존 난이도와 진도 키는 바꾸지 않는다. 하단 탭을 늘리지 않고 퀴즈의 세 번째 트랙으로 둔다. 독립적으로 집필한 문제에 원출처, 교육용 가정과 계산 과정을 제공한다. 사용한 힌트·원답·메모를 progress에 저장하고 정답 여부는 다시 계산한다.

기본 모드는 preview로, 결제나 구매 서버 호출 없이 콘텐츠를 체험한다. 이를 구매한 권한으로 표시하지 않는다. live 모드는 서버의 Google 구매 검증·승인·계정 귀속을 확인하며 스토어에서 조회한 가격을 표시한다. 무료 권한은 서버에서만 부여하고 구매 증명은 백업에 넣지 않는다. 현재 구매 권한은 최대 24시간 내 서버 갱신과 앱 완전 재시작 시 온라인 확인이 필요하다. 장기 오프라인 유료 학습까지 완성했다고 안내하지 않는다. 외부 설정·라이선스 테스트·SMTP·상업용 날씨 계약·심사 접근을 준비한 뒤 유료 판매를 켠다. 앱 설치는 무료이며 기존 기록과 획득 보상을 유료 전환으로 삭제하지 않는다.

## D-065 · 2026-09-10 · 공개 프로필과 천문 아바타 보상

사진과 댓글에 공통 원형 아바타와 공개 닉네임을 표시한다. 기기의 코디를 자동 공개하지 않고 내 마당의 공개 동기화 버튼으로 검증된 외형 선택값 9개와 별칭만 저장한다. 서버 sky_members의 본인 RLS·열별 UPDATE·security invoker RPC를 사용하고 expected_user로 요청 도중 계정이 바뀌는 경우를 거부한다. 이메일·원격 SVG/URL·권한·관측 좌표는 프로필로 보내지 않는다. 공개 아바타를 서버에서 검증한 보상이나 구매 증거로 사용하지 않으며 신고·차단·정지 흐름을 보존한다. 기존 보상 6개에 별 모자 2개와 오리온·달밤·토성·은하 배경 4개를 더한다. 기기에 기록한 관측·학습 업적으로 획득하며 새 공개 선택값은 개인정보처리방침에 고지한다.

D-064/D-065 검증: 소스 `870747c964f81cdec60da06764c70949aad29357`의 beta.13/build18 AAB·APK와 웹앱을 배포했다. Play용 AAB 서명과 개인 APK의 기존 인증서를 검증했고, Pages 34461832598 및 모바일 34461881585가 성공했다. 공개 프로필 마이그레이션을 실제 서버에 적용해 기존 회원·댓글 수와 권한을 확인했다. 구매 서버는 배포하지 않았으며 현재 공개 앱은 preview다. 저장소 읽기 오류의 초안 보존과 구매 권한 복구 후 같은 퀴즈 재개를 회귀 검사했다. Windows WebKit 오프라인 문서 재진입 오류는 미확인으로 남기고 Chromium 대조 성공과 실제 아이폰 검증을 구분한다.

## D-066 · 2026-09-11 · 선행 문제와 문맥 안의 용어 설명

사용자는 '어려운 용어를 짧게 설명한 뒤 문제'라는 해석을 정정했다. 개념을 직접 익힐 선행 문제를 먼저 제공한다. 각 본 문제 앞에 개념 질문과 관계 연습 두 개를 두고, 오답이면 해설 후 재시도한다. 이미 아는 사용자는 본 문제로 이동할 수 있지만 선행 완료나 정답으로 기록하지 않는다. 30개 본 문제의 기존 키·정답·힌트·메모는 보존하고 준비 진도는 별도 키를 쓴다.

어려운 용어는 현재 문제의 정의와 별칭으로 연결하며 길게 누르기·탭·키보드로 설명을 연다. 스크롤 동작으로 설명이 열리지 않도록 이동 취소를 적용한다. 도해는 코드로 정확한 기하·그래프를 구성하고 교육용 가정과 축척을 밝힌다. 수식 변수는 이탤릭, 아래첨자는 모두 직립으로 조판하며 라이브러리와 글꼴은 오프라인 번들에 포함한다.

이야기의 기존 합법적 관측 사진을 재사용하고 두 대상을 함께 담았다는 원문 근거가 있는 이중성단만 공동사진을 쓴다. 나머지는 실제 좌표 도해로 시각화하고 사진과 구분한다. 프리뷰/실결제 설정·서버 데이터를 바꾸지 않는다.

D-066 검증: 소스 `c31c615e89dafeb64af48ee1095d571265d178e9`의 beta.14/build19로 서명 APK·Play AAB를 생성하고 APK와 PWA를 배포했다. 전체701단위·관련Chromium9개·공개웹4개 smoke·Pages34549739499·모바일34549764863 성공. 터치 해제 합성 클릭으로 설명이 닫히던 문제와 조회 재시도 선택 보존·새 제목 초점 이동을 회귀 검증했다. 본 문제 데이터 구조는 이전 버전과 동일하고 준비 진도는 별도다. 실제 폰과 실결제/스토어 심사·Apple 서명은 별도로 남긴다.

### D-067 — 별 관측 화면에 이어지는 지평선과 학습 보상 (2026-09-11)

독립 마당 대신 첫 화면의 지평선을 꾸민다. 남쪽 다섯 방위에 작게 배치하고 0° 위의 천체를 덮지 않는다. 프로필은 같은 지평선과 아바타를 함께 보여주며 댓글은 아바타만 표시한다. SVG 직접 제작·프로필/렌더러 도안 공유·기존 지면 단일 합성으로 오프라인과 센서 성능을 유지한다.

신규 기본 지급은 간소화하고 아바타28개·장식20개·지면3개를 실제 퀴즈/관측 업적으로 해금한다. 이미 제공한 코디와 장식은 회수하지 않고 기존 프로필/코디에 한해 이전 목록을 유지한다. 첫 쓰기에서 ownership-v2 판정을 고정해 새 사용자의 저장/삭제로 잠금이 풀리지 않게 한다. 보상은 DB progress 안에 보존하며 새 과금이나 무작위 뽑기를 추가하지 않는다.

공개 지평선 선택 ID는 명시적인 공개 동기화에만 포함한다. 운영 migration202609110001은 기존 회원 데이터·RLS·구버전 RPC를 유지한다. 위치·전체 관측기록·개인 프로필 이름은 보내지 않고 개인정보 안내에 공개 항목을 반영한다. 렌더·저장·서버 검증의 상세 근거는 docs/HORIZON-PROFILES.md에 기록한다.
## D-068 · 2026-09-11 · 비ASCII 경로 대응을 위한 Gradle 경로 검사 우회

로컬 폴더명이 비ASCII 문자열(`박준형`)인 환경에서 Gradle의 경로 검사 경고가 build 실패 원인이 될 수 있음을 확인했다. 현재 실행 세션의 안정된 빌드 재현을 위해 `android/gradle.properties`에 `android.overridePathCheck=true`를 추가해 경로 검사 비활성화를 적용한다. 영향 범위는 Android 빌드 단계에 한정되며 앱 동작·정렬·서명·자료 포맷에는 변경을 주지 않는다.

## D-068 부록 · 2026-09-11 · build22 산출물 정리

- 기존 서명키/DNS 경로 정책: 로컬 빌드는 `D-061`와 동일한 업로드키(`f5ad3a778d18b33973938b40f5b93f604c0cb099095a9875b09aa4f0c57a98bd`)를 유지한다.
- beta.16/build22는 build21 산출물 동작을 유지하면서 새로 생성한 제출용 AAB이며, `docs/MOBILE-RELEASE.md`의 build22 항목으로 관리한다.
- `android.overridePathCheck=true`는 필요 시에만 유지하고, 추적 가능한 빌드 로그와 함께 `docs/MOBILE-RELEASE.md` 및 `plan/STATUS.md`에 반영한다.

D-067/D-068 배포: 소스 `f4a584442126a855c3a85565201e3e8c610fb984`, `0.1.0-beta.16`/build22 산출물(`0.1.0-beta.16-build22-play-signed.aab`, SHA256 `18ADAE63BB34A736F4C0B8BE90A8ECB3CB44A0E8FCBF3E10F761E005B3324E5D`)의 jarsigner strict 및 bundletool 검증은 로컬에서 통과했다.

## D-069 · 2026-09-11 · 풍경 중심 지평선과 연속 이야기 학습

- 사용자 요청에 따라 꽃·작은 돌·풀 소품을 선택 목록에서 정리하고, 구버전 소유권은 주택·관측 데크·강아지로 이어받는다. 예전 보상 행·아바타·관측 기록은 보존한다. 잔디 마당과 설산·돌산·바다의 원경을 조합하며 첫 하늘과 프로필에 같은 도안을 사용한다.
- 공개 지평선은 기존 네 필드에 선택적인 backdrop enum만 추가한다. migration202609110002는 허용 장식 세 종류와 배경 네 종류만 확장하고 구버전 JSON·RPC·RLS를 유지한다. 운영 서버의 함수 본문 지문을 로컬과 비교했고 기존 회원·사진·댓글·공개 지평선 수와 회원 데이터 지문을 보존했다.
- 업적 48개의 ID·조건·보상을 바꾸지 않고 화면에서 다섯 주제로 묶어 3열 카드와 전체·분류별 달성 수를 제공한다. 데이터 팩 버전은 변경하지 않는다.
- 천체물리는 기존 채점 문항 30개와 개념 질문 60개를 이야기당 9단계로 이어서 보여 준다. 준비/본 문제라는 화면 구분을 없애되 원래 답·힌트·메모·채점 저장 키는 유지한다. 용어는 450ms 길게 누르기로 열고 짧은 탭은 열지 않는다. 키보드 Enter·Space로도 열 수 있다.
- 출시는 beta.17/build23으로 준비한다. 실제 휴대폰 검증과 Play 등록/심사는 자동 검증·AAB 서명과 구분한다.

D-069 배포: 소스 `0287a2b8a5a49275fdb9f7e627a9008f7485962b`, 태그 `v0.1.0-beta.17-build23`. Play AAB 20,849,753bytes·SHA256 `b732d47a80ffd1af91b729423fb2e940c560d2f7afd6bbb843349c8716b0f50b`, 기존 키·전체 서명·bundletool 검증 완료. Pages 34568725207/모바일 34568743773 성공과 공개 APK 바이트 일치를 확인했다. 장 사이 미저장 초안은 문항/풀이 라운드별 메모리에 유지하며 명시 저장 전 DB를 변경하지 않는다.

## D-070 · 2026-09-11 · 발견의 의문으로 돌아오는 천체물리 서사

열 편의 역사 탐구에 한영 도입·90장면·30개 발견 기록·10개 결말을 추가한다. 관측표·사진 건판·지도·스펙트럼·회전 곡선을 읽는 독자를 중심으로 서술하며 실존 인물의 대화·일기를 꾸며 인용하지 않는다. 계산은 각 문항의 기존 교육용 가정을 유지한다.

결말은 세 계산·판단을 각각 한 번 이상 제출하면 열며, 정답 여부와 이야기 감상을 구별한다. 오답을 포함한 실제 해결 수와 해당 장의 복습 경로를 끝 화면에 남긴다. 단계 이동·초안·결말 감상은 점수나 보상을 쓰지 않으며 기존 버전의 풀이·힌트·메모·해금 기록을 보존한다. 마지막 장만 푼 경우에는 첫 미제출 장으로 이어 준다.

출시 번호는 beta.18/build24로 준비한다. 기존 Play 업로드키와 개인 APK 키를 재사용하며 Plus 가격·권한 모드·서버·광고·SMTP에는 변경이 없다. 세부 설명과 역사 근거는 docs/HISTORY-NARRATIVE.md와 docs/HISTORY-QUESTS-SOURCES.md를 따른다.


D-070 배포: 소스 `a0225d33708fb725c7770831ad3fa6aa0402605f`, 태그 `v0.1.0-beta.18-build24`. Play AAB SHA256 `b4a83bf074d6bd4dee68ed4d4406f279751dcf33a5d2941dd4ae2582388874b1`, 개인 APK SHA256 `de33422f0bee82362d95c5f28f9c3560cae6519356223d7b8465f5f8e4e35658`. 기존 서명·전체 payload 검증, Pages 34590649099/모바일 34590650742, 공개 웹앱4검사·APK 재다운로드 일치 완료.

## D-071 · 2026-09-13 · S24+ 실사용 센서와 기록, 기존 Play 키 인계

탭/핀치와 수동 드래그를 구별하고 센서 복귀는 사용자 버튼으로만 한다. Euler 방위각 평활 대신 쿼터니언 필터를 사용하며 천정의 방위 특이점을 자기장 간섭으로 판정하지 않는다. Android/iOS60Hz·화면0.35px 기준 데드밴드·렌더 보간을 사용한다. 자동 나침반과 명시적 비자기식 상대 자이로를 선택하며 상대 모드는 밝은 별 정렬이 필요하고 자력계 소스로 자동 대체하지 않는다.

후면 영상은 로컬 미리보기로만 다루고 저장/업로드/오디오는 없다. 하단 독의 카메라/센서 조작과 안내는 시간 버튼과 겹치지 않는 문서 흐름으로 배치한다. 별 선택은 pointerdown 화면·화면 거리 우선이며 주변 원뿔 밖 별을 정밀 투영하지 않는다. 별 색/크기와 행성 확대 특징은 표시용 근사임을 유지한다.

기록의 과거 평점·시상·투명도 저장/백업은 보존하되 UI에서 제외한다. 공통3개 태그 그룹과 종류별 항목을 보여 준다.8색 스케치의 원본 색을 보존하고 명시적 PNG 내보내기/커뮤니티 초안을 제공한다. 기존 creative 게시 유형을 사용해 서버 스키마를 바꾸지 않는다.

beta.19/build25. 연구실 최신 build24 Play 인증서 f5ad…98bd와 현재 집 PC37e1…6443을 구별한다. 잘못된 키로 서명하거나 새 키를 만들지 않으며, 무서명 AAB·해시·기존 인증서 검증 도구로 인계한다. 상세 근거/실기기 경계는 docs/FIELD-USE-BUILD25.md.

D-071 배포: 소스80eb317, beta.19/build25. Pages34704907732·모바일34704907215 성공. 무서명 AAB SHA256 1f472c9fdf7151ae55e54a030ca19e7a782412e275d435e15ba669e89a6ebfb5,480정적 파일·bundletool·잘못된 키 차단 검증. 연구실 기존 키 최종 서명과 S24+ 실기기 확인은 남는다.

## D-072 · 2026-09-13 · 확대 손떨림 억제와 메인 하늘 안내, 단계별 스타호핑

시야각·화면 픽셀과 220ms 순변위/총 회전량으로 필터 강도를 정한다. 왕복 떨림을 의도적인 빠른 이동으로 해석하지 않으며 쿼터니언 상태와 줌 전환 연속성을 유지한다. 합성 입력에서 85% 이상 RMS 억제는 해당 시험 조건에 한정하며 물리 S24+ 측정치가 아니다. 일반 하늘과 망원경 +Y 추적에 같은 안정화 원리를 적용한다.

상단에는 설정·하늘 설정·시간을 모으고 카메라 조작을 설정에 넣는다. 하단 GPS 센서 버튼 하나가 정지/복귀를 담당한다. GPS 위치와 나침반/자이로 방향의 역할은 설정에서 구분한다. 별도 복귀 패널·자동 복귀 타이머를 추가하지 않는다.

망원경 목표 안내는 #/sky?scope=…의 메인 렌더러 한 개를 사용한다. 카메라 -Z축을 물리 +Y축으로 대응한 quaternion에 기존 정렬을 적용한다. 파인더와 접안 원을 시야 중심에 표시하고, 한 별/두 별 정렬·장비·보조 차트·GoTo 좌표를 유지한다. 실제 경통 추적의 시간은 현재로 고정한다. 보조 화면만 전역 탭 위 포털로 표시하며 독립 코스 화면의 렌더 방식은 바꾸지 않는다.

자동 희미한 별 경로 대신 배우기 안의 6개 큐레이션 코스를 제공한다. 밝은 기준별부터 주변 모양을 확인하고 한 번에 한 구간을 따른다. 넓은 맥락도와 실제 파인더 원을 구별한다. 기존 코스 ID/획득 이벤트는 유지하고 세션 체크포인트를 별도로 저장한다. 지평선 아래·시뮬레이터 단계로 실제 관측 완료를 인정하지 않는다. 경로/자료/물리 확인은 docs/SKY-REFINEMENT-BUILD26.md를 따른다.

beta.20/build26. 기존 데이터 팩·DB·가격/권한·서버는 변경하지 않는다. 연구실 기존 Play 키가 없으므로 검증된 무서명 AAB와 동일 키 인계 도구를 제공하며 서명키를 교체하지 않는다.
