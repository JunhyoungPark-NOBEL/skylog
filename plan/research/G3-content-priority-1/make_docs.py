from pathlib import Path
import json,collections,hashlib
B=Path(__file__).resolve().parent
J=lambda n:json.loads((B/n).read_text(encoding='utf-8'))
data=J('G3-content-priority-1.json');manifest=J('manifest.json');v=J('validation.json');corr=J('catalog-correction-proposals.json')['items'];sources=J('source-registry.json')

def put(n,s):(B/n).write_text(s,encoding='utf-8')

table='| ID | 제목 | 구성 | 확신도 |\n|---|---|---|---|\n'
for i in data:table+=f"| `{i['id']}` | {i['title']['ko']} | {i['facts'][0]['value']} | {i['meta']['confidence']} |\n"
put('README.md',f'''# G3 우선 확충팩 1 — 성단·성운·은하와 길잡이별 20개

작성·조사일: **2026-09-07 (Asia/Seoul)**. 상태: **작성 완료 / 패키지 형식 검사 통과 / 게시 미승인**.

## 이번 범위

G5 선행자료에 지정된 우선 천체 8개를 모두 포함하여, 첨부 121개 대상 안에서 **DSO 17개 + 항성 3개**를 작성했다. 기존 G3 배치 1의 20개와 겹치지 않는다. 누적 **40개 작성, 81개 미작성**이다. 원래 G3는 20개씩 생성하도록 되어 있으므로 이번에도 20개 단위를 유지했다.

원래 배치 2는 항성 전용이다. 이 팩은 G5 선행 조건을 먼저 채우는 **별도 우선 확충팩**이며 `batch-2`로 이름을 바꾸지 않았다. 이후 배치는 대상 ID의 차집합으로 선택해야 하며 알비레오·스피카·알페라츠를 다시 생성하지 않는다.

{table}

## 먼저 열 파일

| 파일 | 용도 |
|---|---|
| `G3-content-priority-1.json` | 새 20개, G3 원래 스키마의 JSON 배열 |
| `G3-content-priority-1.md` | 위와 동일한 배열을 담은 JSON 코드 블록 하나 |
| `G3-content-cumulative-40.json` | 기존 20개를 수정하지 않고 새 20개를 덧붙인 **초안 통합본** |
| `review-items.md` | 거리 충돌, 종류/별자리 키, 관측·출처 검토 사항 |
| `catalog-correction-proposals.json` | 승인 전 수정 **제안**. 원본·콘텐츠에 후보 수치를 자동 적용하지 않음 |
| `raw-selected-values.json`, `catalog-audit.json` | 원본 20행과 필드별 보존·격리·표시 정책 |
| `source-registry.json`, `source-ledger.json` | 출처 URL, 열람 범위, 항목별 주장 계열 및 편집 판단 구분 |
| `guide-checks.json` | 명시 각거리 5개 검사, 콘텐츠가 아직 없는 길잡이 참조 목록 |
| `validation.json`, `validate.py`, `content.schema.json` | 재현 가능한 패키지 검증 |
| `draft-content-index.json` | 초안 40개 목록. **실제 `content/v1/index.json`이 아님** |
| `remaining-targets.csv` / `.json` | 원래 우선순위를 유지한 미작성 81개 |
| `Claude-T6-handoff.md`, `G5-content-readiness.md` | T6 병합 전 검토 지침과 T7 선행 조건 변화 |
| `inputs/` | 받은 카탈로그·대상표·기존 배치 1·G2 별자리 표의 보존 사본 |

## 사실·원값·편집 제안의 구분

첨부 파일은 대상 ID와 수치의 원본이다. `raw-selected-values.json`의 **20행 × 12열 = 240개 셀**과 `inputs/` 원본을 보존했다. 수치의 원본과 과학적 타당성 검증을 혼동하지 않는다.

원자료에는 은하 M31의 거리가 `544`, M51이 `418`로 입력되어 있다. 해당 열의 이름은 `dist_ly`다. NASA 자료와 규모부터 다르므로 이 값들을 사실 설명으로 교육하지 않는다. M13·M3·M15까지 **5개 거리의 숫자 표시를 보류**하고 원값과 외부 대체 후보를 검토 파일에 분리했다. 외부 후보 역시 최신 정밀 카탈로그의 확정값으로 선언하지 않았다.

나머지 기존 거리 12개, 별 분광형 3개, 겉보기등급 20개, DSO 각크기 17개는 입력 문자열 또는 숫자를 보존했다. 빈 거리 3개는 비워 두고 facts에서 생략했다. **모든 `mag`의 측광 대역이 미확인이므로 V등급이라고 쓰지 않았다.** 크기는 원래 수치를 유지하면서 각분 기호로만 표기했다.

요약·이야기에서 물리적 설명과 관측 역사는 NASA·ESA·IAU·연구 기관·원논문 및 명시한 한국 문화 자료에 근거한다. 스토리의 조언·기록법은 편집자의 제안이다. 신화 분량을 채우려고 새로운 전승을 만들지 않았다. 한국 전통 항목은 M45의 좀생이별과 스피카의 제한된 좌각 대응만 넣었다.

`observing`의 **10×50 쌍안경·90mm 굴절 20~80배**, 도시 광해의 한계, 성공 기준은 대상 특성과 일반 관측 원칙에 근거한 보수적 기획이다. 실제 해당 장비로 관측한 결과가 아니다. `bestMonths`는 한국 중위도 저녁의 대표 계절 제안이며 매일 21~24시 고도 25°를 계산해 보증한 결과가 아니다. T7 실제 미션은 천체력으로 다시 선별한다.

사진과 접안 시야, 같은 화면과 실제 물리적 동반 관계, 성군과 현대 별자리, 전체 C14와 개별 NGC 성단을 구별했다. `hopFrom`은 모두 원래 카탈로그에 존재하지만 그 ID의 읽기 콘텐츠까지 모두 작성된 것은 아니다.

## 검증 결과

패키지 검사 **{v['checkCount']}개 중 {v['passedCount']}개 통과, {v['failedCount']}개 실패**. 스키마·문자 수·중복 ID·첨부 셀 보존·후보값 미적용·출처 참조·누적/잔여 목록과 각거리 5개를 검사했다. 하나의 검사에 여러 항목을 묶은 경우는 `validation.json`에 상세 범위를 표시했다.

| 필드 | 실제 최소~최대 | G3 지정 |
|---|---:|---:|
| oneLiner | {v['lengthsUnicodeCodePoints']['oneLiner']['min']}~{v['lengthsUnicodeCodePoints']['oneLiner']['max']}자 | 60자 이하 |
| summary | {v['lengthsUnicodeCodePoints']['summary']['min']}~{v['lengthsUnicodeCodePoints']['summary']['max']}자 | 200~400자 |
| story | {v['lengthsUnicodeCodePoints']['story']['min']}~{v['lengthsUnicodeCodePoints']['story']['max']}자 | 300~600자 |
| facts | {v['factsPerObject']['min']}~{v['factsPerObject']['max']}개 | 5~8개 |

문자 수는 NFC 문자열의 유니코드 코드 포인트 기준이다. JSON 스키마는 기존 배치의 검증 스키마를 유지했다. `meta.generatedBy='gpt-5-pro'`는 요청 스키마와의 호환성 때문에 유지한 문자열이며 실제 실행 모델을 인증하는 값이 아니다.

재검증:

```sh
python -m pip install jsonschema
python validate.py
python validate.py --release
```

첫 실행은 구조가 같다면 종료코드 0을 반환한다. `--release`는 사람의 검토·실제 게시 승인이 없으므로 **의도적으로 종료코드 2**를 반환한다. 자동 구조 검사 통과를 콘텐츠 배포 승인으로 바꾸지 않는다. 이 검증 스크립트는 이 패키지에 대한 검사이며 실제 Claude T6 저장소의 검사기를 실행한 것이 아니다.

## 남은 일과 수행하지 않은 일

새 초안의 확신도는 medium 15개, 거리 격리로 low 5개다. 그렇다고 medium 항목이 자동 게시 승인된 것은 아니다. 거리 오류 후보의 원천 추적, 전체 등급 대역 확인, M42 분류·M5 키 매핑, 일부 전문 출처와 전통 이름 검토가 남았다. 입력 CSV에는 원천 카탈로그 URL·판본·epoch가 없어 그 provenance를 이번 작업만으로 복원하지 못했다.

실제 접안 관측, 앱 기능 검사, T6 검증·병합·커밋, 실제 콘텐츠 인덱스 게시, G5 미션/퀴즈 추가, 전천 skyPick 근접 검사는 수행하지 않았다. 웹 자료의 이미지·PDF·전체 연구 본문·전통 별자리 도형은 패키지에 포함하지 않았다. 출처별 재사용 조건 검토 없이 이 패키지 전체에 특정 외부 라이선스를 일괄 부여하지 않는다.
''')

text='''# 게시 전 검토 목록 — G3 우선 확충팩 1

2026-09-07. **구조 통과와 사실 승인/게시 승인은 별개다.** 외부 자료와 입력이 충돌해도 입력을 조용히 덮어쓰지 않았다.

## 1. 우선 확인: 거리 수치 5개

아래 NASA 수치는 교육용 소개의 근삿값이다. 정확한 최신 합의값이나 단일 교정 정답으로 선언하지 않는다. `dist_ly`라는 입력 열의 단위·원천 열·대상 교차매칭부터 확인해야 한다. 다른 단위로부터의 단순 변환 오류라고 추정해 자동 복구하지 않는다.

| 대상 | 첨부 dist_ly 원문 | 외부 확인값 | 현재 처리 |
|---|---:|---|---|
'''
for c in corr:
 if c['field']=='dist_ly':
  refs=[]
  for x in c['candidates']:
   title,url=x['source'].rsplit(' — ',1)
   refs.append(f"약 {x['value']:,}광년 — [{title}]({url})")
  text+=f"| `{c['id']}` | `{c['originalValue']}` | {' / '.join(refs)} | 숫자 facts 보류, low, 원본 유지 |\n"
text+='''
본문에는 위 원본 수치를 정상 거리로 쓰지 않았고, 외부 후보값으로 바꿔 쓰지도 않았다. 정성적 설명과 관측 안내는 분리해 작성했으므로 원천 값을 바로잡은 뒤 항목 전체를 새로 만들 필요는 없다. `catalog-correction-proposals.json`은 제안 기록이지 실행용 패치가 아니다.

## 2. 원본 필드의 의미 확인

**M42:** `Cl+N (openCluster)`의 복합 표기를 보존했다. NASA가 설명하는 M42는 별 탄생 성운이며 중심에 젊은 별무리가 있다. G5가 parenthetical 문자열만 읽어 산개성단 정답으로 처리하지 않도록 앱 category를 검토한다. 성운을 봤다고 별 네 개를 모두 분리했다고 기록하지 않는다.

**M5:** 입력 `Se1`은 표준 IAU 3글자 코드가 아니다. 문장과 사실표에서는 G2의 뱀자리(`Ser`)와 명시적으로 대응했지만 입력을 수정하지 않았다. 실제 데이터 키 정규화는 T0b에서 승인한다. 거리 28636광년·등급 5.95도 NASA 소개의 약 25000광년·6.7등급과 차이가 있어 정밀 비교 문제에서 제외한다.

**20개 전체 등급:** `mag`의 V 대역 여부, 개별 성분인지 합성광인지, DSO 적분 영역·카탈로그 판본이 없다. 각 값의 원문은 남겼으나 V등급·정밀 밝기 순서 문제에는 사용하지 않는다.

**DSO 17개 각크기:** 수치를 보존하고 각분 단위로만 표시했다. 원자료의 영역 정의가 미확인이므로 실제로 보일 시야원의 지름으로 바로 사용하지 않는다. M51은 주 은하/쌍의 범위, C14는 두 성단 합계/개별의 범위를 특히 구분한다.

**항성 3개 분광형:** `K3II+...`, `B1V`, `B9p`를 원문 그대로 보존했다. 생략 기호를 임의의 동반성 분류로 풀어 쓰지 않았다. 스피카는 소형 망원경으로 분광쌍성의 두 별을 직접 분리하는 목표가 아니다. 알비레오의 넓은 A/B 쌍이 중력적으로 결합됐다고 확정하지 않는다.

## 3. 전통 명칭과 열람 한계

M45의 좀생이보기는 문화적 풍속이다. 농사 결과를 과학적으로 예측하는 법이나 특정 한 구성별의 독점 이름으로 가르치지 않는다. 한국민족문화대백과사전 출처 표기를 유지한다.

스피카의 좌각 대응은 Stellarium `korean`의 특정 공개 재구성에 근거한다. 모든 시대의 한국 문헌에 대한 결론으로 일반화하지 않는다. 전통 데이터셋을 더 복제·병합하는 경우 출처와 재사용 조건을 별도로 확인한다.

M37의 NOIRLab 페이지와 Werner 외 연구, 페르세우스 NOIRLab 페이지, 알비레오 Jack 외 논문은 직접 열기 제한으로 검색 색인에 공개된 범위만 사용했다. `source-registry.json`에서 각 access 필드를 확인한다. M37의 백색왜성·성운 관련 연구가 있다는 설명을 넘는 자세한 수치는 넣지 않았다. 연구 존재 설명도 게시 편집 시 원문을 다시 확인하는 것이 좋다.

## 4. 관측과 참조

문장에 있는 천구의 북/동/서/남은 화면의 위/오른쪽/왼쪽/아래가 아니다. 성단을 찾는 기준 별의 카탈로그 존재는 확인했지만 모든 기준 별의 설명 콘텐츠가 게시된 것은 아니다. `guide-checks.json`의 미작성 읽기 참조를 확인하고, 읽기 카드가 없다고 하늘에서 기준 별을 없애지는 않는다.

10×50·90mm 안내는 실측 성능표가 아니다. 한국 중위도의 `bestMonths`도 날짜별 기상·고도·달빛 판정기가 아니다. T7 미션의 고도 25° 및 21~24시 조건은 실제 앱 천체력으로 다시 검사한다. NGC869·NGC884는 두 개의 개별 ID이지만, 하나의 맨눈 얼룩을 봤다는 이벤트로 관측 둘과 C14 배지를 동시에 자동 부여하지 않는다.

## 5. 항목별 원문 검토 메모

'''
for i in data:
 text+=f"### {i['title']['ko']} — `{i['id']}` ({i['meta']['confidence']})\n\n"
 for n in i['meta']['needsReview']:text+='- '+n+'\n'
 text+='\n'
put('review-items.md',text)

put('Claude-T6-handoff.md','''# Claude T6 전달문 — G3 우선 확충팩 1

아래 내용과 전체 ZIP을 함께 전달한다.

```text
별관찰 프로젝트 T6 콘텐츠 검증·병합을 진행해 주세요.
첨부 G3-content-priority-1-package.zip에는 신규 20개와 원본/검토 기록이 있습니다.

1. 먼저 README.md, review-items.md, catalog-correction-proposals.json을 읽어 주세요.
2. 신규 파일은 G3-content-priority-1.json입니다. 원래 항성 전용 배치 2를 대체하거나
   번호를 재사용하지 말고 plan/research/에 독립 확충팩으로 보존해 주세요.
3. inputs/G3-content-batch-1.json은 수정하지 않은 기존 20개입니다.
   cumulative-40은 초안 통합본이므로 기존 게시 데이터에 무조건 덮어쓰지 마세요.
4. python validate.py와 저장소의 실제 T6 검증기를 둘 다 실행해 주세요.
   패키지 --release의 종료코드 2는 오류 숨김이 아니라 미승인 게시 차단입니다.
5. M31/M13/M3/M15/M51의 dist_ly 원천·단위·교차매칭을 T0b에서 재확인해 주세요.
   외부 후보는 교육용 근삿값이며 승인된 정밀 교정값이 아닙니다.
   low를 일괄 medium으로 올리거나 검토 보류 문구를 실제 거리로 파싱하지 마세요.
6. 전체 mag의 측광 대역·판본·성분/적분 범위를 확인해 주세요.
   V 확인 전 숫자 비교 퀴즈 또는 고립 후보 brightness cut에 사용하지 마세요.
7. M42의 복합 type→앱 nebula 매핑과 M5 Se1→Ser 참조 키를 명시적으로 검토하세요.
   raw-selected-values와 입력 CSV는 기록용 원문 그대로 남겨 주세요.
8. G2 표시명 정책, 전통 명칭, 부분 열람된 전문 출처, 관측 문구를 검토해 주세요.
9. hopFrom의 카탈로그 ID는 유효하지만 일부 읽기 콘텐츠는 아직 없습니다.
   실제 content/v1/index.json에는 검토·게시 승인된 항목만 넣어 주세요.
10. 승인/보류 항목과 사유를 보고하고, 오류 수정 요청은 해당 ID와 검사 메시지로 반환하세요.
    G5는 실제 게시 인덱스를 확인한 뒤 미션·퀴즈를 확장해야 합니다.
```

## 후속 배치 선택

원래 첨부는 121개다. 이번 확충 후 미작성은 별 17개·별자리 30개·DSO 34개로 총 81개다. `remaining-targets.csv`는 원래 priority 순서를 유지한 차집합이다. 이미 작성한 3개 별 또는 17개 DSO를 새 배치에 다시 넣지 않는다.

이 문서는 실제 Claude 실행 결과나 커밋 완료 보고가 아니다.
''')

put('G5-content-readiness.md','''# G5 선행 콘텐츠 변화 — 작성 상태만 갱신

2026-09-07. 기존 `next-content-priorities.md`의 “미작성” 항목 8개가 이번 팩에 모두 작성되었다. **게시 완료로 바뀐 것은 아니다.** G5 기존 JSON 4개는 이번 작업에서 수정하지 않았다.

| 새 항목 | 확장할 수 있는 학습 주제 | 먼저 확인할 조건 |
|---|---|---|
| M45 | 알데바란→V자 별무리→플레이아데스 성단 스타 호핑, 좀생이별 이야기 | T6 게시 승인, 실제 날짜/장비 시야, 관측 완료 이벤트 |
| M44 | 봄 쌍안경 산개성단, 맨눈의 얼룩과 별무리 비교 | 카탈로그 대역과 실시간 가시성 |
| M42 | 겨울 성운과 중심 별무리의 구별 | 앱의 nebula 분류 승인, 별 네 개 자동 관측 처리 금지 |
| M31 | 가을 은하 관측, 사진과 접안 시야 | 거리 충돌 해소. 당분간 거리 문제 금지 |
| M13 | 초여름 구상성단, 산개/구상성단 비교 | 거리 충돌 해소. 90mm 별 분해 보장 금지 |
| 알비레오 | 시각적 이중성, 색 대비, 초점·배율 | A/B 중력 결합을 정답 사실로 쓰지 않음 |
| 스피카 | 봄 길잡이, 처녀자리 소속, 분광쌍성과 시각 분리 차이 | skyPick는 전천 근접/대역 검증, 전통 명칭 범위 |
| 알페라츠 | 가을 길잡이, 성군과 공식 별자리 | skyPick 전천 근접 검사 |

추가 12개는 행성상성운 M57/M27, 산개성단 M35/M37/NGC869/NGC884, 구상성단 M3/M5/M15, 은하 M81/M82/M51의 비교 문항·관측 후보를 제공한다. 어두운 은하 관측을 Bortle 7~8 필수 성공 과제로 강요하지 않는다.

## 스타 호핑 튜토리얼의 콘텐츠 근거

M45의 `howToFind`에는 오리온 허리띠로 방향을 잡고 알데바란 주변 V자 별무리를 확인한 뒤 북서쪽 M45로 이동하는 순서가 있다. 첨부 RA/Dec로 알데바란→M45의 각거리 약 13.652°와 북서 방향을 검사했다. 이 각거리는 한 쌍안경 시야 안이라는 뜻이 아니다. 앱에서 현재 시야 원과 중간 별 배열을 사용해야 하며 `skill:starhop` 완료와 실제 `observe`를 분리한다.

M45 콘텐츠가 실제 인덱스에 게시되면 기존 G5의 보류된 네 번째 튜토리얼을 구현할 근거로 사용할 수 있다. 이번에는 Mission JSON을 임의로 만들어 실행 가능하다고 표시하지 않았다.

## 숫자·skyPick·배지 제한

이번 20개만으로 G5의 최종 150~200문항 및 skyPick 30문항 조건을 충족한 것이 아니다. 은하·성단은 밝고 고립된 점광원 skyPick의 숫자를 채우는 대체재가 아니다. 별자리 소속·물리적 종류·찾기 관계처럼 본문에 근거하는 정성 문항을 우선 검토하되, 정확한 수치·밝기 순서 문제는 카탈로그 승인 뒤 만든다.

전체 C14와 두 NGC 성단, M51과 동반 은하, M42와 트라페지움의 개별 관측 이벤트를 구별한다. 원형 선택 영역에 들어왔다고 다른 대상까지 봤다고 기록하지 않는다. 실제 T7 인덱스, 전체 표시 천체와 동적 달/행성 근접, 3° 판정, 날짜별 고도 25° 조건 검사는 남아 있다.
''')

registry='# 출처 목록과 실제 사용 범위\n\n확인일 2026-09-07. 읽지 못한 전체 본문을 읽었다고 표시하지 않았다. 웹 이미지·논문·PDF 원문은 첨부하지 않았다. 원자료는 `inputs/`에 별도로 있으며 소스의 물리적 설명을 원자료 수치의 자동 인증으로 사용하지 않는다.\n\n'
for s in sources:
 registry+=f"## {s['key']}\n\n[{s['title']}]({s['url']})\n\n열람: {s['access']}.\n\n사용 범위/주의: {s['notes'] or '본문의 해당 대상 설명을 요약·재서술.'}\n\n"
put('sources.md',registry)
put('requirements.txt','jsonschema>=4.18,<5\n')
print('Docs created; remaining categories',dict(collections.Counter(x['id'].split(':')[0] for x in J('remaining-targets.json'))))
