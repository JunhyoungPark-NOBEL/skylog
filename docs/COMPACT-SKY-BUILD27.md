# 간결한 하늘 조작 — beta.21 / build27

2026-09-13. 최신 main3268f9d와 원격 일치를 확인하고 시작했다. Galaxy S24+ Android 설치 앱 사용자의 배치 요청을 반영했다.

## 구현 요약

- 왼쪽 위에는 설정 아이콘 하나, 바로 아래에는 아래꺾쇠 메뉴를 배치했다. 메뉴를 누르면 하늘·검색·오늘 밤·기록·배우기가 아래로 펼쳐진다. 콘텐츠 화면의 하단 탭은 유지한다.
- 설정에는 하늘·GPS·앱 탭을 두었다. 하늘 레이어, 지면·별 표시, 영상 밝기/시야, GPS 상태/보정, 앱 테마·언어·장비·관측지를 모두 제공한다.
- 오른쪽 위 시계 아이콘은 눌렀을 때만 날짜·시간·배속 조절을 펼친다. 시간 이동 중에는 작은 점과 접근성 이름으로 상태를 표시한다. 망원경의 실제 방향 안내에서는 시간 이동 잠금을 유지한다.
- GPS 버튼은 하단 중앙, 카메라는 오른쪽 아래에 둔다. GPS 연결 실패·권한 도움·정렬 문구가 하늘을 덮지 않는다. 오류 원인은 설정에서 확인할 수 있다. 후면 영상은 직접 켜며 저장/전송/마이크 사용 없이 기존 로컬 미리보기를 유지한다.
- GPS는 새 앱 실행 시 기본 자동 연결을 시도한다. 사용자가 끄면 그 실행 중 탭 왕복에서도 꺼진 채 유지한다. OS 권한이 없거나 센서를 지원하지 않으면 활성 상태로 가장하지 않는다. 지면 투명도 기본은0%(불투명도1), 기존 조절값은 보존한다.

## 검증

typecheck·전체 lint·단위772개·PWA 빌드 통과. 전체 브라우저113개 중112개 통과 후, 통합 설정의 이전 테스트 절차와 하늘을 유지한 채 야간 전환 시 CSS/렌더 팔레트 갱신 순서 문제를 수정했다. 최종 하늘·메뉴·설정16개 재검사 모두 통과, 별/지면의 적색 픽셀도 확인했다. 360px·200% 글자에서44px 터치 영역과 메뉴·GPS·카메라 간격을 확인했다. 공개 배포·모바일 산출물은 후속 기록한다. UI는360px,200% 글자, 영문/야간, 메뉴 키보드/밖 누르기, 카메라·시간·설정 동선과 신규 실행/이전 저장값을 확인한다. 기존 손떨림 필터·별 선택 유지·+Y 망원경 정렬·코스/기록/학습은 회귀 검사 대상으로 둔다.

## S24+ 확인 항목

- 앱을 완전히 종료하고 다시 열어 허용된 GPS 방향 추적이 시작되는지 확인한다.
- 설정 아래 메뉴에서 다섯 화면으로 이동하고 하늘로 돌아온다. 직접 GPS를 끈 뒤에는 같은 실행 중 계속 꺼져 있는지 확인한다.
- 우상단 시계의 시간 조절/현재로 복귀, 우하단 카메라 켜기/끄기와 설정 안 영상 조절을 확인한다.
- 확대 추적·별 선택·수동 이동, 망원경 시야 원/보정이 이전처럼 동작하는지 확인한다.
- 신규 설치 또는 하늘 설정 기본값 복원 후 지면 투명도가0%인지 확인한다. 이미 조절한 사용자 값은 유지한다.

## 산출물과 남은 경계

결정 D-073. DB·데이터 팩·서버/요금/계정 정책은 바꾸지 않았다. 새 GPT Pro 자료는 필요하지 않다. 물리 S24+ 센서 감각은 사용자가 확인한다.

이 PC에는 연구실 Play 업로드키가 없어 무서명 AAB와 동일 인증서 검증 도구를 제공한다. 기대 인증서 SHA256은 f5ad3a778d18b33973938b40f5b93f604c0cb099095a9875b09aa4f0c57a98bd다. 연구실에서 기존 키로 서명해야 하며 무서명 AAB는 Play 제출 완료본이나 휴대폰 직접 설치 파일이 아니다.
